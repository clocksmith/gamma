# Requirements for the JAX engine
# JAX installation is platform/accelerator specific. Refer to JAX documentation:
# https://github.com/google/jax#installation
# Example CPU: pip install jax[cpu]
# Example CUDA 12 (ensure compatible jaxlib): pip install jax[cuda12_pip] -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html

jax>=0.4.23
jaxlib>=0.4.23
flax>=0.8.0
# `transformers` and `numpy` are in the base requirements.txt