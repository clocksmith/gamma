# Requirements for the JAX engine
# JAX installation is platform/accelerator specific - see JAX docs
# Example CPU: jax[cpu]>=0.4.23
# Example CUDA 12: jax[cuda12_pip]>=0.4.23 -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
jax>=0.4.23
jaxlib>=0.4.23 # Often installed alongside JAX based on platform
flax>=0.7.5
# transformers is in the base requirements.txt
# optax>=0.1.7 # Often used with Flax for optimization, but not strictly needed for inference here
