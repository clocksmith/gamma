# vLLM Engine Requirements
# High-performance inference engine with PagedAttention and continuous batching
# Requires NVIDIA GPU with CUDA support

# Core vLLM package
vllm>=0.3.0

# PyTorch (required by vLLM)
torch>=2.0.0

# Transformers (for tokenizers and model configs)
transformers>=4.35.0

# Additional dependencies
numpy>=1.23.0
