# Codex controlled-session failed-attempt receipt

Session: `codex-controlled-session-2026-08-09-v2`

Evidence: **failed LLM simulation diagnostic**, not a human playtest

All four participant unboxing records completed. Two participants completed
the monolithic four-document rules-reading prompt. One Codex process then
reached the preregistered `300000` ms provider timeout, and the run failed
closed before facilitation or gameplay. No game decision or outcome exists for
this attempt. The stage journal and structured failure record preserve the
completed boundary.

The locked successor `codex-controlled-session-2026-08-09-v3` preserves the
same release, ruleset fingerprint, seed, factions, profiles, model, reasoning
effort, and provider timeout. It replaces the monolithic rules prompt with four
document-specific readings and a separate synthesis record.
