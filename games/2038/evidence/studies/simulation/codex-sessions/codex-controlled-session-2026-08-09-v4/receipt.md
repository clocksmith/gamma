# Codex controlled-session failed-attempt receipt

Session: `codex-controlled-session-2026-08-09-v4`

Evidence: **failed LLM simulation diagnostic**, not a human playtest

All four unboxing records completed. Three participants completed Core Rules
reading: two on their first provider attempt and one on an identical second
attempt. The fourth participant exhausted both preregistered attempts at the
`300000` ms guard. Both failures are retained in the journal. The run failed
closed before the remaining documents, facilitation, or gameplay, so it has no
game outcome.

The locked successor `codex-controlled-session-2026-08-09-v5` keeps the same
bounded retry policy and splits every frozen rules document into lossless
source chunks of at most 8000 characters. Source chunk identity, part count,
and inherited heading travel with every reading and facilitator record.
