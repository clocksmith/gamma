{
  "name": "color-palette-generator",
  "description": "Build a color palette generator using the dream.js color library - tests real-world library integration",
  "category": "full-projects",
  "difficulty": "hard",
  "promptLevels": {
    "novice": "generate color palette from hex color",
    "beginner": "make a PaletteGenerator class that uses dream.js library to create color palettes",
    "intermediate": "create a PaletteGenerator class that imports Hct and themeFromSourceColor from dream.js to generate Material Design color schemes from a hex color",
    "advanced": "write a PaletteGenerator class that uses dream.js library to generate Material Design 3 color palettes from hex colors, with methods to generate a scheme and export it as CSS or JSON",
    "expert": "Write a PaletteGenerator class that integrates with the dream.js color library to generate Material Design 3 color palettes. The class should: 1) Import Hct and themeFromSourceColor from the dream.js library. 2) Have a generateFromColor(hexColor) method that takes a hex color string (e.g., '#FF5733'), uses themeFromSourceColor to generate a complete Material Design color scheme, and returns a ColorScheme object with properties like primary, secondary, tertiary, etc. (use the structure returned by dream.js). 3) Have an exportAsCss(scheme) method that takes a ColorScheme and converts it to CSS custom properties format (e.g., '--md-sys-color-primary: #...'). 4) Have an exportAsJson(scheme) method that returns the scheme as a plain JavaScript object. 5) Validate the hex color input and throw descriptive errors for invalid inputs. The dream.js library provides Material Design 3 utilities based on the HCT (Hue, Chroma, Tone) color space for creating accessible, harmonious color palettes. {languageSpecific}"
  },
  "languageInstructions": {
    "typescript": "Import types from dream.js if available, or define your own ColorScheme interface based on the structure returned by themeFromSourceColor (with properties like schemes.light.primary, schemes.dark.primary, etc.). Use proper TypeScript types for all methods. The generateFromColor method should validate hex format with regex and throw TypeError for invalid inputs. Export the class and interfaces.",
    "javascript": "Import {Hct, themeFromSourceColor} from dream.js (or '../dream.js'). Implement hex validation using regex (/^#[0-9A-F]{6}$/i). Use the scheme structure returned by dream.js theme generation. Export the class as default.",
    "javascript-jsdoc": "Use JSDoc with @typedef for ColorScheme (based on dream.js return type), @class for PaletteGenerator, @param {string} hexColor for generateFromColor, @returns {ColorScheme}, @throws {TypeError} for invalid hex colors. Document the dream.js integration and return structures. Export the class."
  },
  "variants": {
    "typescript": "Create a TypeScript module that uses the dream.js color library (located at ../dream.js) to generate Material Design color palettes. Requirements:\n\n1. Import necessary functions from dream.js:\n   - Hct (color space)\n   - themeFromSourceColor\n   - Any other utilities needed\n\n2. Create a PaletteGenerator class with:\n   - generateFromColor(hexColor: string): ColorScheme\n   - generateFromImage(imageData: ImageData): ColorScheme[]\n   - exportAsCss(scheme: ColorScheme): string\n   - exportAsJson(scheme: ColorScheme): object\n\n3. Proper TypeScript interfaces:\n   - ColorScheme interface\n   - PaletteOptions interface\n   - Export types\n\n4. Error handling for invalid colors\n\n5. Export the class and all types\n\nNote: The dream.js library provides Material Design 3 color utilities based on HCT color space.",
    "javascript": "Create a JavaScript module that uses the dream.js color library to generate Material Design color palettes. Requirements:\n\n1. Import from dream.js (Hct, themeFromSourceColor, etc.)\n\n2. Create a PaletteGenerator class with:\n   - generateFromColor(hexColor)\n   - generateFromImage(imageData)\n   - exportAsCss(scheme)\n   - exportAsJson(scheme)\n\n3. Error handling for invalid colors\n\n4. Export the class\n\nSame functionality as TypeScript version.",
    "javascript-jsdoc": "Create a JavaScript module with JSDoc that uses the dream.js color library. Requirements:\n\n1. Complete JSDoc type definitions:\n   - @typedef ColorScheme\n   - @typedef PaletteOptions\n   - @typedef HctColor\n\n2. PaletteGenerator class with full JSDoc:\n   - Document all methods with @param and @return\n   - Use @template where appropriate\n\n3. Same functionality as other variants\n\n4. Export with JSDoc @exports",
    "javascript-vanilla-web": "Create a color palette generator web application using dream.js. Include:\n\n**HTML:**\n- Color input field\n- Image upload button\n- Generated palette display (color swatches)\n- Export buttons (CSS, JSON)\n- Preview section showing colors\n\n**CSS:**\n- Modern, clean design\n- Color swatches in a grid\n- Hover effects on swatches\n- Copy-to-clipboard visual feedback\n\n**JavaScript:**\n- Import and use dream.js library\n- Generate palette from hex color input\n- Extract colors from uploaded image\n- Display color swatches with hex values\n- Click to copy color values\n- Export as CSS variables\n- Export as JSON\n\n**Features:**\n- Real-time palette generation\n- Visual color preview\n- Material Design 3 compliant colors\n- Accessible color combinations\n\nReturn as single HTML file or clear structure. Must actually use the dream.js library functions.",
    "javascript-vanilla-web-jsdoc": "Same as javascript-vanilla-web but with:\n- Complete JSDoc for all functions\n- @typedef for all data structures\n- Document the dream.js integration\n- Type-safe callback functions",
    "typescript-vanilla-web": "Create a color palette generator using TypeScript and dream.js with vanilla Web APIs:\n\n**TypeScript:**\n- Import dream.js types\n- Type-safe color handling\n- Proper DOM typing\n- Interface for app state\n\n**Features:**\n- Type-safe event handlers\n- Generic color utilities\n- Strongly-typed palette generation\n- Type-safe exports (CSS, JSON)\n\n**HTML/CSS:**\n- Modern UI design\n- Responsive layout\n- Color accessibility indicators\n\nProvide TypeScript source and HTML structure.",
    "typescript-react": "Create a color palette generator using TypeScript React and dream.js:\n\n**Components:**\n- App (main container)\n- ColorInput (hex input with validation)\n- ImageUploader (upload and extract colors)\n- PaletteDisplay (show generated colors)\n- ColorSwatch (individual color with copy)\n- ExportPanel (CSS/JSON export buttons)\n\n**Custom Hooks:**\n- usePalette (manages palette state)\n- useColorExtractor (extract from images)\n- useClipboard (copy to clipboard)\n\n**TypeScript:**\n- Interface for ColorScheme from dream.js\n- Props types for all components\n- Type-safe dream.js integration\n\n**Features:**\n- Real-time palette generation\n- Image color extraction\n- Copy colors to clipboard\n- Export functionality\n- Material Design 3 compliant\n\nMinimal deps (React + dream.js only)."
  },
  "testCases": [
    {
      "name": "Class instantiation",
      "test": "const generator = new PaletteGenerator(); console.assert(typeof generator === 'object', 'Generator should be an object');"
    },
    {
      "name": "Generate from color",
      "test": "const generator = new PaletteGenerator(); const scheme = generator.generateFromColor('#FF0000'); console.assert(scheme && typeof scheme.primary === 'number', 'Scheme should have a numeric primary color');"
    },
    {
      "name": "Export to CSS",
      "test": "const generator = new PaletteGenerator(); const scheme = generator.generateFromColor('#0000FF'); const css = generator.exportAsCss(scheme); console.assert(typeof css === 'string' && css.includes('--md-sys-color-primary'), 'CSS export should be a string with CSS variables');"
    }
  ],
  "requirements": [
    "dream.js",
    "color palette",
    "Material Design",
    "HCT",
    "generate",
    "export",
    "integration"
  ],
  "performanceBenchmarks": [
    {
      "name": "generate-palette",
      "code": "for (let i = 0; i < 100; i++) { const baseColor = \"#\" + Math.floor(Math.random()*16777215).toString(16).padStart(6, \"0\"); const palette = [baseColor]; for (let j = 1; j < 5; j++) { palette.push(\"#\" + Math.floor(Math.random()*16777215).toString(16).padStart(6, \"0\")); } }",
      "iterations": 50,
      "baselineTimeMs": 5.0
    },
    {
      "name": "convert-color-formats",
      "code": "const hexToRgb = hex => {const r = parseInt(hex.slice(1,3), 16); const g = parseInt(hex.slice(3,5), 16); const b = parseInt(hex.slice(5,7), 16); return {r,g,b};}; for (let i = 0; i < 1000; i++) { hexToRgb(\"#\" + Math.floor(Math.random()*16777215).toString(16).padStart(6, \"0\")); }",
      "iterations": 50,
      "baselineTimeMs": 10.0
    }
  ]
}
