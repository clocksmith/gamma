---
description: Compare model outputs side-by-side
allowed-tools: Bash
argument-hint: <model1> <model2> [--prompt TEXT]
---

Run model comparison mode to see side-by-side output from multiple models.

```bash
python3 gamma.py compare --models $ARGUMENTS
```
