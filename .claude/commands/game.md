---
description: Run the token prediction game
allowed-tools: Bash
argument-hint: [--model MODEL] [--difficulty LEVEL]
---

Run the token prediction game with optional parameters.

```bash
python3 gamma.py game $ARGUMENTS
```

If no arguments provided, run with defaults:
```bash
python3 gamma.py game
```
