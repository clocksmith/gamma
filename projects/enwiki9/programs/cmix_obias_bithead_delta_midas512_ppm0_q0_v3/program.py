"""Composite Gamma DELTA-MIDAS plus exact PPM residency candidate.

This candidate composes the sealed v2 bit-head overlay with the sealed
budget-zero PPM build contract. The implementation closure and compile-time
arm mapping are frozen in ``composition.json``.
"""
