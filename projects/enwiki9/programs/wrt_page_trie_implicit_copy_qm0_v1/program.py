"""Manifest stub for the WRT page-trie paid trace screen."""


def compress(data: bytes) -> bytes:
    raise RuntimeError("QM0 is a trace-priced coded-alphabet gate, not a native codec")


def decompress(archive: bytes) -> bytes:
    raise RuntimeError("QM0 requires a passing exact-replay successor")
