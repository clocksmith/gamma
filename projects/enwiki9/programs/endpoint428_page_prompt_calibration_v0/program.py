"""Manifest entry for the SIBYL V0 exact page-prompt trace shadow."""


def compress(data: bytes) -> bytes:
    raise RuntimeError("SIBYL V0 is a zero-credit trace shadow before native integration")


def decompress(archive: bytes) -> bytes:
    raise RuntimeError("SIBYL V0 has no constructive archive at the shadow stage")
