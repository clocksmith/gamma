{
  "id": "xml_skel_wc3tier_jpvitm_z_lzma2_1g_v1",
  "description": "score-honest gzipped-source variant: jpvit + binary archive manifest. JSON manifest replaced with compact binary framing (1B mode + 8B size + 32B raw sha256 + N varint per-channel sizes); per-channel sha256 hashes dropped (total hash check at the end suffices). Saves ~850 B pre-lzma per archive vs JSON manifest",
  "added": "2026-05-10",
  "deps": []
}
