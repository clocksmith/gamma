"""Descriptor for the second source-built full-1G cmix-obias roundtrip.

The executable proof is
``tools/cmix_obias_source_full1g_roundtrip_b_qm0.py``. It runs in an isolated
result and scratch namespace from arm A so the two terminal full archives can
be compared byte-for-byte after both exact inverses pass.
"""
