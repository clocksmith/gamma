"""Descriptor for source-built full-1G determinism and score accounting.

The executable receipt is
``tools/cmix_obias_source_full1g_determinism_accounting_qm0.py``. It runs only
after both independent source-built full-corpus roundtrip arms terminalize,
requires identical payload and archive identities, and charges the exact
program, head, archive, and conservative invocation bytes.
"""
