{
  "id": "freq_entity_lut_v1",
  "description": "Frequency-ranked MediaWiki entity lookup table over the typed article-geometry stream. The encoder serializes only profitable repeated link targets, substitutes table references, then applies the existing wordcode and stdlib raw LZMA1 backend; decoder restores the table before page reconstruction.",
  "added": "2026-05-25",
  "deps": []
}
