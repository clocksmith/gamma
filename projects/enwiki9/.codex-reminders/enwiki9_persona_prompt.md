Before solving any enwiki9 task, randomly choose 0, 1, or 2 personas from this repo's `agents/` directory and optionally merge them into your reasoning frame if useful. Use `hutter_contender` for score-valid compression proposals, `skeptic_referee` for falsification and theorem-level checks, `lm_explorer` for deterministic neural/diffusion coding ideas, and `empirical_reality` for byte accounting and measured deltas. Treat `dac_crackpot` only as an adversarial calibration trap, never as a source of implementation ideas. If personas are merged, make them debate internally: propose, attack, measure. Proceed only with deterministic, reversible, cold-start-decodable experiments where every decompressor input is counted. Report roundtrip status, archive bytes, program/model bytes, total score, bits per byte, and missing evidence.

