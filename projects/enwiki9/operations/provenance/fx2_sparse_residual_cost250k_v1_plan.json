{
  "bound": "G=sum(x),H=sum(x*x). SinceF_second<=-4H/9, F(a)<=aG-2a*aH/9. Exact maximum atabs(a)=min(1/2,9abs(G)/(4H)); zero whenH=0. Add exact conservative rowQ16-rounding allowance. For each active count k, sum k largest row bounds then subtract8ceil((16+20k)/8). Max overk0..8 is an upper on all complete sparse-table ideal gains.",
  "caps": {
    "cpus": [
      3
    ],
    "memory_bytes": 1073741824,
    "scratch_bytes": 16777216,
    "swap_bytes": 0,
    "wall_seconds": 360
  },
  "configurations": 1,
  "controls": "Native feature/coder encode,decode and raw-repeat files remain byte-identical; all data and dictionaries rehashed. Exact WRT inverse and all trace truths checked. Recompute first moments and match prior closed bound. Two independent certificate subprocesses must produce identicalJSON.",
  "decision": "Nonpositive exact paid upper closes this sparse-family ideal economics on this population. Positive upper authorizes bounded fitting only; no sufficient gain,finite-archive or larger-scope claim. No additional table encoding sweep under this gate. A fitted candidate must pay complete implementation and survive independent confirmation.",
  "feature": "Unchanged verified32 ternary hidden features from fx2_residual_features250k_v3. No feature regrouping or new native inference.",
  "id": "fx2_sparse_residual_cost250k_v1",
  "model": "Each bit-position row is absent or chooses one of32features and one nonzero signed coefficient with magnitude1..16384,scale32768. Parent state fixed; q=p+p(1-p)*a*phi_j, same Q16 rounding as the dense proposal.",
  "objective_credit_bytes": 0,
  "owner": "root_sparse_residual",
  "population": "Exposed raw[0,250000),151210 modeled WRTbytes,1209680bits; no confirmation.",
  "schema": "gamma.enwiki9.sparse-residual-plan.v1",
  "table": "Versionbyte1,8presence bits (MSB-first row0..7),then active rows in order. Each entry5feature bits,1sign bit,14magnitude-minus-one bits. Final zero padding to a byte. All information paid,2..22bytes. Code/runtime cost not omitted from any final score; they are deliberately excluded from this optimistic futility bound.",
  "unit": "operations/provenance/fx2_sparse_residual_unit_20260915.json"
}
