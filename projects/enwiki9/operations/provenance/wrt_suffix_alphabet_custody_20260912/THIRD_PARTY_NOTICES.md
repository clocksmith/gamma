# Source and license inventory

- The cmix 21 sidecar sources and `english.dic` are distributed under GNU GPL
  version 3. The complete license is in `COPYING.cmix`.
- `wrapper.c` and the package Makefile are Gamma project work distributed under
  the MIT license in `LICENSE.gamma`.

The package deliberately uses only the submitted source and dictionary plus
the system C/C++ toolchain. It does not include or require Zig or liblzma.
