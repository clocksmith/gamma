# Operator Queues

Queue files in this directory are durable inputs to operator tooling.

Queue consumers should use explicit paths or defaults rooted at
`operations/queues/`. Completion logs and launcher output belong in
`run_logs/`.
