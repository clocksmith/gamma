"""Synthetic MIDAS observer parity/cost identity; execute its bound tool job.

The implementation is tools/midas_open_observed_gate_v1.py with this candidate's
prospective contract. This diagnostic identity has no score-bearing codec API.
"""


def compress(data: bytes) -> bytes:
    raise RuntimeError("Use the frozen MIDAS observed tool job; no score-bearing codec API")


def decompress(archive: bytes) -> bytes:
    raise RuntimeError("Use the frozen MIDAS observed tool job; no score-bearing codec API")
