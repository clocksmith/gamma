{
  "schema": "gamma.enwiki9.wiki-schema-vm-interface.v1",
  "candidate_id": "wiki_schema_vm_ceiling_q0_v1",
  "role": "causal_shadow_opportunity_and_association_screen",
  "archive_authority": false,
  "score_credit_bytes": 0,
  "population": {
    "bytes": 587138826,
    "sha256": "7826ff63dedd526c119dda08e6e044be8fa8f6e89a55f3d6b1f3447cdfc5c1ce",
    "coordinate": "post-WRT transformed byte"
  },
  "alphabet": {
    "P": "transformed left brace",
    "Q": "transformed vertical bar",
    "M": "transformed equals",
    "R": "transformed right brace"
  },
  "parser": {
    "template_open": "PP",
    "template_close": "RR",
    "field_boundary": "top-frame Q",
    "named_key_end": "top-frame M before value state",
    "maximum_stack_depth": 32,
    "maximum_template_name_bytes": 48,
    "maximum_field_key_bytes": 48,
    "maximum_staged_programs_per_template": 64,
    "overflow": "suppress the over-depth subtree or overlength atom without prediction or learning until its causal delimiter closes; a nested template inside a template name invalidates that complete outer name",
    "staging_overflow": "discard every staged learning update from that invocation; predictions remain based only on earlier completed invocations"
  },
  "program_table": {
    "sets": 32768,
    "ways": 4,
    "candidates_per_state": 2,
    "state": "exact current template-name hash, previous completed field-key hash or positional marker, and zero-based field index",
    "target": "next named field-key bytes followed by transformed equals M",
    "update": "stage at most 64 state/target observations inside one invocation, discard the complete staged set on staging overflow, and update deterministic two-candidate Space-Saving heavy hitters in source order only after that template invocation closes",
    "selection": "candidate lower-bound support at least two and strict majority of the state's completed observations",
    "set_replacement": "lowest observation count, then lowest source-ordered way",
    "dynamic_allocation_after_construction": false
  },
  "treatment": "At a decoded field boundary, replay the selected next-key program until completion or the first treatment mismatch.",
  "controls": {
    "K": "identical parsing, lookup, state writes, counters, collisions, and hashes with prediction disabled",
    "R": "splitmix64 bytes at the exact D opportunity positions",
    "S": "program selected from the immediately preceding completed template name with the same prior-key and field-index coordinates, truncated or cyclically repeated to D's target length; if absent, rotate D's target by one byte so activation and length remain matched"
  },
  "gates": {
    "one_byte_per_correct_volume_screen_bytes": 4079243,
    "one_byte_per_correct_volume_screen_is_futility_authority": false,
    "maximum_parent_actual_bit_surprisal_from_quantization_bits": 16,
    "maximum_optimistic_log_loss_gain_bytes_per_active_byte": 16,
    "minimum_active_opportunity_bytes_for_target_scale_envelope": 254953,
    "required_optimistic_log_loss_upper_bound_bytes": 4079243,
    "all_chronological_thirds_positive": true,
    "treatment_must_beat_random": true,
    "treatment_must_beat_shifted": true,
    "repeat_identity_required": true
  },
  "next_gate_on_pass": "retained-parent donor-surprise trace over the exact D opportunity manifest; the trace, not correct-byte count, decides whether target-scale parent surprisal exists",
  "terminal_on_miss": "retire this exact output-only schema-program information source if its 16-byte-per-active-byte quantized log-loss envelope is subscale, association controls fail, a third is empty, repeat identity fails, or bounded state fails; do not use the one-byte-per-correct diagnostic as futility authority"
}
