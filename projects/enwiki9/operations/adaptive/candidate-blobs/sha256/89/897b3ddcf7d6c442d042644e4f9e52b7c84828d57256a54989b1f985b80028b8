"""Gamma bit-head DELTA-MIDAS v2 candidate descriptor.

The executable diagnostic is
``tools/cmix_obias_bithead_delta_midas512_q0_v2.py --arm ARM``. It exports the
pinned GPL-3.0 cmix-obias source, applies ``overlay.py`` only after exact source
identity validation, builds one compile-time arm, and requires repeat encode
plus exact bare inverse.
"""
