"""Zero-credit exact-memory-policy successor for the cmix-obias substrate.

The implementation is the compile-time definition frozen in
``build_contract.json``. The diagnostic runner builds the unchanged tracked
source with that definition, then requires repeat encoding, exact inversion,
and process-tree RSS compliance.
"""
