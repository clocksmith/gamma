{
  "schema": "gamma.enwiki9.candidate-interface.v1",
  "candidate_id": "endpoint428_horizon_retained_parent_trace_exact_recovery_q0_v3",
  "role": "zero-credit reflected orphan-recovery exact integer reanalysis",
  "inputs": ["validated and reflected orphan-adoption result", "sealed immutable parent.p1", "immutable manifest-a.bin", "frozen v1 legacy analyzer", "frozen v2 exact analyzer"],
  "changed_axis": "Replace only the impossible terminal-v1 evidence intake with the reflected orphan-adoption seal; preserve the v2 63-step unsigned-__int128 quotient and exact half-up remainder test.",
  "preserved": ["manifest format", "trace format", "population", "D/S/R/N donors", "KT schedule", "candidate quantization", "mixture count", "global posterior scope", "third boundaries", "scientific gates"],
  "execution_barrier": "The runner must reject before reading parent.p1 or manifest-a.bin unless v1 has no running or pending job, has decision.json, and has a valid reflection bound to that decision hash.",
  "archive_authority": false,
  "score_credit_bytes": 0
}
