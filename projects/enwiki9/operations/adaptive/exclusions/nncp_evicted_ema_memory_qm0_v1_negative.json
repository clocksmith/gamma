{
  "evidence": [
    "results/nncp_evicted_ema_memory_qm0_v1/decision.json"
  ],
  "exclusion_id": "nncp_evicted_ema_memory_qm0_v1_negative",
  "failure": "Candidate saved only 13 actual archive bytes against the frozen 800-byte gate; aligned ideal gain was 12.114 bytes despite exact identity and positive but negligible thirds.",
  "mechanism": "One fixed decoder-built decay-0.5 EMA of the oldest 64 exact per-layer hidden states stored in one of the existing 256 NNCP memory slots",
  "population": "Exact 65536-symbol constructive incremental-KV NNCP prefix with two encoders, one decoder, official inverse, and faithful 96142-byte baseline",
  "recorded_at": "2026-08-08T22:33:13+00:00",
  "retired_dimensions": [
    "Do not sweep EMA decay, summary slot count, pooling rule, or context length around this fixed compressed-memory coordinate."
  ],
  "schema": "enwiki9_omega_exclusion_v1",
  "unsettled_successors": [
    "A materially different learned state replacement remains distinct only with independently measured mature same-domain headroom."
  ]
}
