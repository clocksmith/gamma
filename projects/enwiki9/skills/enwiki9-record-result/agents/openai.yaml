interface:
  display_name: "Record enwiki9 Result"
  short_description: "Bind one result to the proof frontier"
  default_prompt: "Use $enwiki9-record-result to record this receipt-backed enwiki9 result with the supplied disposition."
