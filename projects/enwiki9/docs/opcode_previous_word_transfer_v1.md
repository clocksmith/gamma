# Previous-word transfer comparison

Owner `root_explore`. Continue the validated
[release result](../operations/provenance/opcode_previous_word_release_terminal_20260909.json)
without changing the predictor. The release preserves147 development archive
bytes, costs110 more local source bytes than P per copy, and remains unqualified.

Use exactly the [event-parse validation population](../operations/provenance/opcode_event_parse_validation_population_v1.json):
raw offsets614250000 through614499999, SHA256
`ffb6c9e73f59dc3ee7109441aa05881d1980ff440f47db63120bf36822b765bf`.
Initialize cold. This sample was used by other mechanisms; it is separate from
word development, not globally unseen confirmation data. Do not substitute the
different field-validation sample also called reserved250KB. Retained P is68981
bytes and its wrapped audit supplies the authoritative parent projection.

The [runner](../tools/opcode_previous_word_transfer_gate_v1.py) reuses the
authenticated13-phase word comparison with the compact observer. It runs P,
bookkeeping-only K, immediate-word D and delayed-word S through independent
encode/decode/repeat processes, then encodes D without observation. P/K archives
and parent projections must agree; all arms retain the same parse and update
counts; K/D/S histories agree. Complete within-arm witnesses must synchronize.

Four additional processes exercise the delivered release in a separate output
directory. Its archive and complete audit must equal newly measured compact D,
with an exact inverse, deterministic repeat and unobserved archive equality.
Those fresh D artifacts are ordered phase dependencies, never claimed as frozen
inputs that existed before execution. Grammar, source, framing and keys stay fixed.

Require D smaller than both P and S for predictive transfer. Report D's archive
saving minus110 source bytes and minus220 separately. A positive one-copy local
subtotal supports another bounded research gate; it proves neither accepted
package economics nor a complete90M witness. The5961-byte control implementation
is experiment machinery; the5856-byte release is the delivered local source.
Runtime, licenses, options and required multiplicities remain unresolved.

Before corpus execution, freeze source, materialization, runtime, evidence,
population and controls; publish ownership and the held job; verify publication
and refresh host admission. Assign CPU2,4GiB aggregate memory, zero swap,1GiB
scratch and4200 elapsed seconds. Each of17 phases retains the existing2GiB address,
180 CPU seconds,240 elapsed seconds and32MiB file limits. Reserve32GiB host
headroom. These are execution stops, not engineering estimates or qualification.
Historical opening measurements provide the resource basis, not transfer results.

First run bounded synthetic runner tests on CPU3. Any missing mandatory evidence,
state divergence or budget stop blocks a scientific transition. A valid loss
rejects this fixed transfer result without disproving all word-conditioned models.
Do not launch1MB automatically, tune on this sample, modify HORIZON/MIDAS, or
install dependencies. Preserve every failed attempt and exact source binding.
