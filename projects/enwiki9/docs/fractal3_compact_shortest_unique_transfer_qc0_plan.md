# FRACTAL-3 compact shortest-unique transfer QC0

Status: terminal zero-credit rejection. The exact compact result is
`89,993.155` displaced bytes, only `55.753` bytes above Endpoint428 QM5.

## Objective

Reprice QM5's decoder-causal shortest-unique whole-event trigger spans on the
archive-identical compact-only Endpoint428 replacement trace. This changes only
the parent probability substrate; it does not search rules, prefixes, controls,
thresholds, or partitions.

## Frozen gate

The compact source-bound forecast is `109,499,618` bytes, or `4,499,618` above
the current `105,000,000` target. At canonical `10M`, compact QC0 must reach at
least `100,000` displaced bytes, remain positive in every chronological third,
and exceed F0, E0, C0, and S0 by at least `20,000` bytes. This also requires at
least `10,000` bytes of substrate uplift over QM5's `89,937.402` Endpoint428
ceiling. Failure retires this transfer without tuning.

## Evidence boundary

The parent compact trace must have a receipt proving trace-on/reference archive
identity and clean decimal-memory guards. QC0 inherits only QM5's exact causal
span construction. Rule definitions, source identities, and invocations remain
free at this gate, so any positive result earns zero score credit. A pass may
authorize one separately frozen paid replay; only exact deterministic replay
retaining at least `60,000` net bytes after all counted costs may authorize
native compact integration.

## Result

The `10M` compact observer/reference archives are byte-identical at `1,636,331`
bytes with SHA-256 `d6d2d725...`. Both decimal-memory guards pass. QC0 is
positive on every third and retains every control margin, but fails both core
gates: J0 is below `100,000`, and uplift over Endpoint428 is below `10,000`.
Retire this transfer without a paid replay or nearby substrate/rule tuning.
