# Previous-word treatment release

Owner `root_explore`; a separate delivery identity from the controlled
[compact implementation](opcode_previous_word_compact_v1.md). The controlled
bundle remains5961 bytes,215 above the original parent. Its six synthetic tests
pass; no corpus savings are inherited by the compact realization yet.

The [release builder](../tools/opcode_previous_word_release_build_v1.py)
authenticates the compact packed source and original323-byte parent loader.
It removes only P/K/S selection, fixes the immediate-word treatment, and retains
the local disabled-history prefix estimator. It rejects any remaining `_arm`
dependency. The delivered encoder and decoder require only `p` and `program.py`
plus the disclosed Python/runtime dependencies; construction and observation
tools are not needed for unobserved operation. Complete accounting is unresolved.

Before corpus parity, require exact synthetic archives/inverses/repeats and
complete probability/update/parse/history witnesses against the controlled D
implementation, independent relocated operation through the delivered loader,
tracking-sentinel checks, deterministic materialization and rejection of changed
inputs or existing output directories. The diagnostic adapter may add arm D to
an observation namespace; the unobserved release must never require it.

Synthetic bounds: six existing fixtures totaling1173 raw bytes, CPU3,512MiB
address space,120 CPU seconds,180 elapsed seconds,32MiB per output file.
No corpus launch or full-package credit follows from export size alone.

The [five-test release receipt](../operations/provenance/opcode_previous_word_release_unit_20260909.json)
verifies all treatment archives and full witnesses on the six fixtures, plus18
isolated encoder/relocated-decoder/repeat processes using only the delivered
files. Local source is5856 bytes:1167 below the adapter and110 above the original
parent per delivered copy. The [corpus parity runner](../tools/opcode_previous_word_release_gate_v1.py)
replays opening250KB against the retained original treatment archive and full
audit before any inherited archive comparison. Its four phases are observed
encode/decode/repeat and unobserved encode. One-copy local-source accounting and
two-copy sensitivity remain separate from complete official package accounting.
