# Correction of the fixed profile's implicit dictionary

The v1 auxiliary gate is retained as an incomplete comparison. It reached a
segmentation fault in the original parent's extracted decode; the trimmed
PPM treatment was not executed. The separate immutable-binary diagnostic
isolates whether ambient `.dict` activates a different legacy FXCM predictor.
This successor cannot be admitted without that diagnostic's validated result.

Both source arms receive the identical correction: remove the constructor's
implicit `dosym()` call. The historical static-profile archives were encoded
without `.dict` in their working directory. The corrected profile specifies
that same predictive behavior regardless of working-directory files. Its
explicit frontend dictionary, dictionary pretraining, model weights, coder,
learning, vocabulary and auxiliary algorithms are unchanged. Only one source
file changes relative to each v1 arm; the exact preimage is checked.

Repeat the frozen v1 scope and all29phases under a new identity. Preserve
CPU2, memory9999998976B, zero swap, logical scratch16GB and aggregate7200s stop.
Each corrected arm must produce the retained33429-byte main archive, independently
invert and repeat, and build identically twice. Auxiliary dictionary/order
roundtrips, actual embedded extraction and restricted main decode all remain
required. The isolated decoder deliberately retains `.dict`; renaming it is
not the fix. Archive comparison uses the corrected parent and treatment only.

The two component packaging forms remain alternatives. Measure every new
binary/container/ZIP instead of inheriting previous reductions. A source or
resource failure is incomplete; nonpositive component economics do not invalidate
an exactness repair. No full split/PHDA/reorder execution, complete package,
eligibility or full-corpus score is asserted by this bounded fixture.
