# cmix-obias geometry-order host-repacked Qm0 v2

Status: frozen infrastructure successor; external-parent archive screen; zero
score credit

Candidate:

```text
cmix_obias_geometry_order_host_repacked_qm0_v2
```

## Frozen scientific contract

This successor preserves the exact scientific hypothesis, canonical
opening-10M population, `B0` public-order parent, `G0` Gamma geometry order,
conditional `T0` title control, repeated-G0 determinism check, `5,000`-byte
gross archive gate, `32,768`-byte source allowance, promotion boundary, and
kill neighborhood from:

```text
docs/cmix_obias_geometry_order_substitution_qm0_plan.md
```

It changes only the malformed auxiliary-asset bootstrap found by attempt v1.

## Attempt-v1 infrastructure failure

Job `20260803T020929Z_31a36cad90` used the exact public packaged binary and
precompressed `comp_order`/`comp_dict` assets. Both auxiliary decodes reached
their documented output lengths, but the current AMD host reconstructed a
non-integer order line and the public `reorder()` routine aborted in `stoi`
before corpus scoring. The parent payload and archive were never produced.

The memory guard was not the cause:

```text
maximum sampled single RSS       7,984,788 KiB
maximum sampled process-tree RSS 7,992,936 KiB
decimal-10GB excess                      0 KiB
```

The donor documentation warns that its arithmetic streams are tied to the
binary's PGO/CPU floating dialect. Attempt v1 is therefore an infrastructure
failure with no scientific verdict.

## Sole v2 correction

Use the same exact as-built raw executable prefix for every operation, but
locally recompress the two raw bootstrap assets before constructing any arm:

```text
raw dictionary
  bytes       411,996
  SHA-256     4c8568cca9343b9a6212477880f56f8efd162f8784224a25edd043097d36215a

raw public order
  bytes       1,094,862
  SHA-256     eecd462c29319bab185b48229c4d09ab52f16ca9c582e8e32eff9a7c2a7de39e
```

The local dictionary compression is repeated in two clean directories and
must be byte-identical. `B0`, `G0`, and conditional `T0` all use that one
verified local dictionary stream. Each asset is compressed with the exact
fp16 head enabled, matching the environment inherited by the auxiliary
decoders during corpus encode and bare archive decode. Each arm's order stream
is compressed by the same raw executable on the same host. Thus encoder,
auxiliary decoders, payload decoder, and archive stub share one exact
machine-code and model-launch dialect.

Because host repacking may change the parent payload, v2 does not require the
external `1,599,218` payload or `1,882,538` archive sizes. Parent validity is
instead established constructively by:

```text
exact bound source/raw-asset identities
byte-identical repeated local dictionary stream
B0 bare archive decode to exact opening 10M
G0 bare archive decode to exact opening 10M
```

The scientific statistic remains strictly paired:

```text
G_archive_10m = bytes(B0 archive9) - bytes(G0 archive9)
```

No external full-1G score is adopted. Any 1G projection remains an explicitly
zero-credit planning diagnostic because host repacking can shift the full
parent.

## Decision

`AUTHORIZE_SOURCE_CHILD` requires:

```text
all donor/raw-asset/input identities             exact
local dictionary compression replay             byte-identical
B0 bare roundtrip                                exact
G0 order rebuild                                 byte-identical
G0 bare roundtrip                                exact
G0 archive gain                                  >= 5,000 bytes
G0 archive                                       < T0 archive
second G0 payload/archive                        byte-identical
```

A valid miss returns `REJECT` with zero score credit and retires the exact
geometry-order/backend/source-envelope family. A malformed host-local asset,
roundtrip, guard, or subprocess result returns nonzero without a scientific
verdict. No key, model, PGO, memory, or order-codec sweep follows either
outcome.
