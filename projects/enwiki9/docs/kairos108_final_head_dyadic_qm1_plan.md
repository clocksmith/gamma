# KAIROS-108 final-head dyadic opening gate QM1

Candidate: `kairos108_final_head_dyadic_qm1_v1`

Parent: `kairos105_final_head_dyadic_qm0_v1`

Status: frozen no-overwrite infrastructure repair; zero score credit.

## Single change

The parent reached its corrected observer source but never executed scientific
work because the adaptive queue created an empty canonical result directory
before the tool called `mkdir`. This child uses unique result and external build
paths. It accepts only an empty queue-created canonical directory, removes that
empty directory, and then delegates to the parent's frozen observer, feature,
dyadic-selection, matched-control, and arithmetic-replay implementation. Any
nonempty canonical directory or any existing external directory still fails
closed.

No probability feature, rank, quantization, atomic interval, correction law,
dynamic program, control, schedule cost, arithmetic coder, donor asset, or
opening gate changes. The historical parent patch remains a content-addressed
source dependency. The canonical research target is `108,000,000` counted
bytes, but this opening experiment grants no forecast or score credit.

## Required arms and decision

Run unchanged `B0`, `G0`, `K0`, `P0`, `R0`, `S0`, and `O0` over the first
`1,000,000` raw bytes. Require traced-parent payload identity, exact `B0` and
`K0` range replay, repeat identity, legal probabilities, and a paid schedule no
larger than `128 KiB`. The discovery references remain `4,500` gross bytes and
`500` bytes over every matched control, positive in every chronological third.

Passing the complete opening gate authorizes only one compact full-stream
statistics observer. Failing scientific gates retires this KAIROS realization;
an infrastructure failure may be retried only by another no-overwrite identity
that changes no scientific mechanism.
