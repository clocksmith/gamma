# Atlas-Clockwork Seal-2 Binding Audit

Classification: `ORGANIZER ONLY`
Audit date: `2026-07-26`
Decision: `UNBOUND`
Authorized candidate routes: none

## Source-bound starting point

The target is an exact counted score at or below `108,000,000` bytes on exactly
`1,000,000,000` input bytes with successful roundtrip. Current evidence does
not establish an exact full-corpus baseline:

- Verified full-1G score: unknown.
- Best counted forecast: `109,389,323`.
- Forecast-only distance above target: `1,389,323`.
- Operational status and frontier forecast disagree.
- Source and arithmetic validation fail because required evidence is missing.

Forecasts cannot discharge a hypothesis-and-margin certificate.

## Route A: rejected

Problem A optimizes selector and description costs for an already supplied gain
matrix and explanation codebook. Binding requires an exact full-corpus
baseline, legal causal explanations, exact jointly replayed block gains,
complete representation costs, and target margin after prefix loss.

The canonical map would select the lexicographically first maximizing
assignment and minimum-price prefix code. No qualifying gain matrix or
target-bearing codebook exists. The theorem allocates known gain; it creates no
predictive information. Proving the missing antecedent would already certify a
winning construction.

## Route B: rejected

Problem B constructs behavioral quotients, distinguishing words, Wheeler
orders, and continuation intervals. Binding requires exact class probabilities,
complete model/index costs, a full-corpus codelength implication, and resource
bounds.

The canonical map would construct the unique quotient and lexicographically
first valid order or fixed-depth unfolding. Wheeler geometry provides indexing
but no positive codelength theorem. Gamma's causal Wheeler shadow is measured
negative.

## Route C: rejected

Problem C bounds fixed-point error and excess logistic loss for a contractive
recurrence. Binding requires an exact under-target full-corpus teacher, global
Euclidean contraction, state and factor bounds, score slack covering loss and
package growth, and official resource sufficiency.

The canonical map would choose the smallest satisfying dyadic precision and
lexicographically first factorization. Gamma has no exact under-target
full-corpus teacher; the best value is a forecast with unqualified runtime and
failing source validation.

## Route D: rejected

Problem D isolates a low-ranked object in an energy-ordered candidate space.
Binding requires exact true-block ranks, a search-decomposable energy, complete
syndrome/framing costs, bounded decoder expansions, and full-corpus margin.

The canonical map would choose the lexicographically first successful matrix at
minimum depth. The theorem separates an object after low rank is known; it does
not create low rank or feasible search. General MOIRAI evidence is negative.

## Conclusion

All routes are removed from candidate authorization. They remain in the problem
bank for expert mathematical review only. Seal-2 may become `BOUND` only after
at least one hidden reduction supplies every missing antecedent and passes the
committed verifier.
