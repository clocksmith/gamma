# Frozen native trimming confirmation

The opening250KB release comparison established an unchanged33429-byte archive
with a438792-byte trimmed executable versus483848 bytes originally, and a
3905220-byte source ZIP versus3910568. The separate learned mixture and other
corrections do not inherit these gains. The delivery-selection receipt prefers
trim-only P, but requires its own confirmation contract before execution.

Candidate `fx2_trim_confirm1m_v1` tests that exact selected implementation,
without a predictor change, source minimization search or coefficient fitting.
This is the workflow's testing branch after a paying component result, not a
new compression hypothesis. The goal remains90000000 complete bytes.

Use the pre-existing raw interval[713000000,714000000), cold initialized, from
`opcode_field_confirmation1m_v1.raw`. Its SHA256 is
`20b4d8d7e140ccf799ed9af127d03a42af006efc97bd38e3cf2528cd611aaf13`.
The population was already used for native FX2 and opcode comparisons. It is
separate from trimming development, not globally untouched. Its retained
131238-byte native archive is an exact reference, not a selectable target.

P rebuilds the original delivered source ZIP. D rebuilds the previously frozen
trimmed P source ZIP. Both must reproduce their authenticated executables and
repeat a build after make clean. No source mutation occurs in this gate.
Both independently preprocess, encode, decode and reencode restored raw data.
All archives must match the retained reference byte for byte; reconstructed
raw bytes and preprocessing must match across arms. These are14 phases.
The executable contains the native predictor; no supplied counts, probability
rows or teacher state are runtime inputs. Paid trained weights and dictionary
remain required and byte-identical. No optional probability dump is generated.

The eight changed source members and four removed LSTM headers are already
frozen by the delivery ZIPs. Existing three synthetic runner tests verify exact
ZIP reconstruction, the entire changed/removed member set and paid asset
identity, and rejection of unsafe/nonregular source entries. Native build and
inverse evidence are separate and cannot be inferred from those tests.

For this fixed confirmation, require unchanged archive bytes, executable
reduction45056 and source-ZIP reduction5348. Source and executable forms are
alternatives, not summed. Shared models/dictionaries, runtime, notices,
invocation options, official form and multiplicities still require closure.
All comparisons failing identity or reconstruction invalidate confirmation;
resource failure is not a compression regression. No full-corpus credit.

Native internal probability and optimizer state are not serialized here.
Archive identity and inverse tests cover this finite population. The existing
source argument restricts the removal to the fixed transformer execution path;
it is not a measured universal or cross-CPU equivalence claim.

One worker on CPU2, inherited from lab-controller creation;9999998976 bytes
memory, zero swap,16000000000 logical scratch bytes and3600-second aggregate
stop. Compilation stops at360 seconds per phase and codec execution at600.
The native sparse PPM scratch is14680064001 logical bytes and is deleted only
after the owning child exits. Both arms use separate source/build directories.
Timing remains diagnostic. A passing result authorizes package closure and a
separately frozen next-scale gate, not an automatic full1G launch.

Two source-only alternatives were checked while selecting this action. The
existing KVF32 layout only expands already-quantized int8 values, as recorded
in the previous premise audit. Attention sparsification is also already
disabled: all production calls in the pinned parent pass skip_threshold0.0f.
Changing either setting therefore supplies no established new information.
Higher-precision prequantization inference would be a different, unmeasured
model with no inherited gain. None of these alternatives is launched here.
