# NNCP LibNC Train-Length-32 Mature 1998848 Qm2

Status: frozen source-native maturity screen

## Purpose

The coupled native `train_len=32, d_pos=288` profile saved 829 bytes at 10,000
symbols and decoded exactly. Qm2 asks whether that source-native cadence gain
survives after training from symbol zero through the existing mature
1,998,848-symbol population.

## Frozen gate

Reuse the exact preprocessed stream, dictionary, binary, library, vocabulary,
four-thread schedule, and encode-only mode of the faithful 2,042,820-byte
receipt. Change only training length to 32 and its required relative-position
width to 288. Produce one guarded archive.

Require at least 30,000 actual archive bytes and decimal-memory compliance.
This is a screen: encode-only output cannot be submitted or decoded, and a
pass authorizes repeated, decodable confirmation rather than score credit.

A miss retires mature promotion of this built-in surrogate without training-
length, relative-width, batch, learning-rate, retraining, thread, or adjacent-
population sweeps. It does not retire the distinct exact KV-rebuild child.
