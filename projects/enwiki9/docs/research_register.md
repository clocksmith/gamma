# enwiki9 Research Register

This register tracks strategy and algorithm research separately from measured
candidate proof. It is a map from idea to local implementation, not a scoreboard.

Claim rule:

```text
Research status is not compression proof.
Promotion requires exact receipts: result JSON, shadow-coder receipt, or guard
receipt depending on the lane.
```

Strategy pivot: the calibrated `fx2_geometry_sort_dictcmix_xz_zlibpy_min_v1`
forecast remains the prize-facing baseline. The unchanged FXCM2/full-buffer
`cmix21` package and fixed PAQ-free `96x2` endpoint are both retired from the
promotion queue by exact archive economics. The PAQ-free result changes the
mechanism priority: its bounded substrate remains useful, but the next lane
must add new causal information. The old CMIX21 complement used original
article order and is not additive to geometry `96x2`. The corrected
archive-neutral matched replay is terminal: its best exact full-endpoint blend
retained `290 B/1M` overall and `355 B/1M` on internal holdout, below the
`500 B/1M` integration screen. Individual mixer interactions and continuously
evolved 160/200-cell probes also miss the screen. A separate standalone
`200x2` construction saves `1,149 B/1M` at the first `1M`, but retains only
`370 B/1M` on the offset-`500M` reset slice against its `762.424 B/1M` floor.
The terminal `112+80` wrapper is similarly narrow: it retains only `4 B/1M`
over native `112` and `353 B/1M` total on that slice. A reproducible
`264,646`-byte source package saves `23,619` counted
bytes and moves the same tail forecast to `109,498,879`, but does not repair
the disjoint failure. A static WRT dictionary boundary swap is separately
retired after worsening the exact `10M` archive by `1,140` bytes. Recurrent
capacity remains teacher evidence while selector and width
work over the recorded geometry endpoints is retired. The exact explicit
WRT phrase-copy macro is also terminal: every active development configuration
lost bytes and the frozen holdout action was abstention. A newly matched
original-order replacement screen is stronger: exact FX2 plus compact `200`
saves `1,345 B/1M` overall and `1,325 B/1M` on internal holdout with zero
holdout regressions. That signal is not additive to geometry ordering and the
two probability streams are not yet a counted one-process decoder, so a frozen
cumulative `10M` transfer screen is the active gate. SRSTC
remains relevant only when a new WRT-native form supplies independent
probability against that substrate. `cmix21`,
`fx2`, residual/SSE, schema tries, embedding
teachers, MWCC, and I-SSA remain teacher lanes or components; they are not
promoted without counted target-substrate receipts.

## Novelty Portfolio Contract

Do not let the target search collapse into an indefinite sequence of mixer,
weight, width, or reset-policy adjustments.  Classify every experiment as:

1. substrate tuning;
2. a new probability endpoint;
3. a new reversible representation;
4. a new coder or explicit event type.

After a terminal economics miss, a tuning lane receives one matched-trace
decision, not a native weight ladder.  A second native gate requires positive
evidence from a new endpoint, representation, or coder.  Keep at least three
orthogonal mechanisms in the cheap oracle or shadow stage while one candidate
is being integrated.

Speculative mechanisms use this funnel:

```text
decoder and inverse contract
  -> noncausal headroom plus matched control
  -> causal shadow or exact reversible transform
  -> counted disjoint-prefix implementation
  -> guarded native gate
  -> full official proof
```

An oracle may kill an expert universe but cannot promote a selector.  A failed
selector cannot retire an explicit-copy, grammar, graph, columnar, or delta
representation that creates different coded events.

`docs/wikiir_mdl.md` defines the primary orthogonal compiler program.  Its
first probes are parameterized Wiki tree grammar, prior-page prototype delta,
and a typed token Sequence Memoizer with Skip-CTS control.  WebGraph-style
typed list coding and nested columnar page blocks follow.  Article-order TSP
and generic residual-DAG search are demoted until these new representations
produce paying signal.

The endpoint428 lane is bounded by this contract: observation identity, one
matched larger-scope trace, then freeze one paying causal policy or retire the
family.

## Active And Proposed Lanes

| Lane | Foundation | Local files | Current evidence | Promote gate | Kill gate |
|---|---|---|---|---|---|
| Retired FX2-SC sidecar geometry-title gate | Minimized FX2-SC byte-split/direct/extra/page-match backend plus exact measured reversible geometry-title transform | `programs/fx2_sidecar_geometry_title_dictcmix_zlibpy_min_v1/`, `results/fx2_sidecar_geometry_title_dictcmix_zlibpy_min_v1/10m_target_ceiling_abort.json` | Terminal canonical `10M` ceiling abort: native-output lower bound `1,641,762`, `10,181` bytes above the `1,631,581` promotion ceiling before final flush. Memory was healthy with max sampled tree RSS `6,072,528` KiB and `3,693,097` KiB decimal-`10GB` margin. | None unchanged; re-enter only through a different residual/layout mechanism that first clears target-substrate evidence. | This exact integration shape is retired. |
| SRSTC / streaming self-referential retrieval | Causal k-nearest continuation memory over deterministic SimHash/minhash sketches, self-referential span tables, patch-copy priors, and fixed-point regret routing | `docs/streaming_retrieval_mixer.md`, `tools/streaming_retrieval_mixer_plan.py`, `tools/streaming_retrieval_shadow.py`, `tools/streaming_retrieval_raw_shadow.py`, `tools/streaming_retrieval_block_regime_audit.py`, `programs/srstc_raw_order2_aggregate_sketch_blockposterior_v1/`, `results/streaming_retrieval_shadow/` | The `65,536K` raw block-posterior receipt saves `916,540` held-out and `900,464` net bytes with zero regressions. Its complete `4,805,936`-row fx2 transfer saves `5` overall but loses `4` held-out and is `-16,080` after code, so the unchanged aggregate expert is retired for fx2 integration. | A replacement expert predicts fx2 residuals directly or improves reversible layout, clears counted cost on held-out target-substrate evidence, and then survives exact native replay. | Requires shipping an embedding/index payload, exceeds memory, or remains non-positive against the strong substrate after counted cost. |
| WikiIR-MDL reversible compiler | Exact typed Wiki/XML IR with literal, grammar, prior-page delta, graph, columnar, citation/reference, Sequence-Memoizer, and Skip-CTS descriptions selected by complete MDL | `docs/wikiir_mdl.md`, the `programs/wikiir_*` candidates, and `tools/wikiir_*` probes | Sixteen exact serialization families fail target economics. Citation-field columnarization loses on every LZMA scope; named-reference interning has only an `11,363`-byte full-corpus raw ceiling; same-skeleton reference retrieval exposes `125,092` causal raw-MDL bytes but exact layouts lose. The follow-on causal WRT reference-prefix CTS also loses `1` exact FX2 byte on an event-dense `500K` selection window, and its positive-event oracle is only `0.072266` byte. | Retire every measured serialization and the measured reference-prefix endpoint unchanged. Preserve exact parsers for a representation that creates genuinely new target-residual information rather than another reference continuation selector. | Exact inverse costs consume gain, mature backend context already captures the oracle, or target-residual replay remains non-positive after code. |
| SAME-R target-substrate capability transfer | Frozen exact codelength evaluator with anchor, targeted, and matched-control lanes; noncausal oracle ceilings screen candidate universes before causal routing | `docs/same_r_hutter_strategy_audit.md`, `docs/cmix21_fx2_family_trace_correction.md`, `../../samer/README.md`, quarantined matched-trace receipts | Qwen fixed32, WRT token-span, family-average, individual-mixer, nested-LSTM, and final-CMIX endpoint forms are retired. The exact matched geometry blend saves `290 B/1M` overall and fails the `500 B/1M` screen. | A new WRT-native endpoint beats anchor/control on untouched rows after complete code/table cost, then survives native integration. | The new endpoint universe misses the headroom floor, targeted does not beat control, hidden state reaches decode, or native transfer consumes the gain. |
| Retired current-title echo and WRT title-transition endpoints | The pre-WRT shape rewrites current-title phrases; the no-rewrite successors predict WRT event continuations from current-title transitions, hierarchical support/backoff, and current-minus-previous contrast. | `tools/random_window_novelty_screen.py`, `tools/random_window_fx2_title_echo_gate.py`, `tools/wrt_exact.py`, `tools/wrt_title_token_automaton.py`, `tools/wrt_title_support_backoff.py`, `docs/random_window_novelty_v1.md`, and quarantined receipts under `/home/x/enwiki9-nonproof/results/wrt_title_token_automaton_endpoint428_1m_v1/` | Pre-WRT rewriting loses `347` native bytes at `1M`. The corrected no-rewrite scorer exactly reconstructs the raw prefix and endpoint428 payload. Its best hard rule estimates `+1.350 B/1M` but loses `2` exact bytes. The `1,065.488 B/1M` current-title positive-event oracle is close to its `919.426 B/1M` previous-title control and does not transfer causally. All `108` support/backoff variants lose; best is `-11.102 B/1M` and `-9` exact bytes across `2` positive and `8` regressing blocks. | None unchanged. Preserve the exact decoder, but require a new event representation such as residual-native retrieval or typed-field Skip-CTS before revisiting title information. | Terminal target-substrate economics: both destructive rewriting and no-rewrite transition probabilities fail, and selector tuning cannot create missing information. |
| Retired typed Skip-CTS endpoint | Hierarchical integer count/backoff over exact WRT event prefixes, optional Wiki field/mode/slot histories, and endpoint428 probability buckets | `tools/wrt_typed_skip_cts_trace.py`, `tests/test_wrt_typed_skip_cts_trace.py`, and quarantined receipts under `/home/x/enwiki9-nonproof/results/wrt_typed_skip_cts_endpoint428_1m_v1/` | After removing an event-length causality leak, the legal global form saves `45` exact bytes overall but only `1` proportional-holdout byte with `6/16` blocks regressing. Slot/order-3 saves `3` overall and `7` holdout with `4/16` blocks regressing. Both are below the frozen `69.404 B/1M` floor before source cost. | None unchanged. A successor must create a different probability endpoint and repeat the completed-byte-only causality audit. | Legal causal gain remains below target economics; earlier apparent `940`-`14,198 B/1M` gains exposed eventual event length and are invalid. |
| Retired entity-node residual backoff endpoint | Existing causal title/link trie plus hierarchical calibration keyed by decoder-built node, prior support, event-relative bit, retrieval probability, and endpoint428 confidence | `tools/wrt_entity_trie_fx2_shadow.py`, `tools/wrt_entity_node_backoff_trace.py`, `tests/test_wrt_entity_node_backoff_trace.py`, and quarantined receipts under `/home/x/enwiki9-nonproof/results/wrt_entity_node_backoff_endpoint428_1m_v1/` | On the exact `4,805,936`-row endpoint428 trace, node-specific calibration loses `8` exact bytes overall and `1` holdout; the global-backoff control loses `9` overall and `3` holdout. Both are causal and use completed entity state only. | None unchanged. Retrieval must create a different endpoint universe rather than another selector or calibration over these trie continuations. | Terminal target-substrate economics are negative for both node-specific and global controls. |
| Retired direct semantic-event SRSTC endpoint | Bounded continuation table over suffix/SimHash/schema/hybrid sketches of decoder-rebuilt raw/Wiki state before each completed WRT event, followed by a compact self-trained base/endpoint/bit calibration control | `tools/wrt_event_srstc_trace.py`, `tools/cmix_aux_bucket_calibration.py`, their tests, and quarantined receipts under `/home/x/enwiki9-nonproof/results/wrt_event_srstc_endpoint428_1m_v1/` | The raw continuation endpoint loses `920` exact bytes overall and `188` holdout bytes, with all `16` holdout blocks regressing. Its only permitted probability-bucket calibration loses `31` overall and `5` holdout bytes. | None unchanged. A successor needs a different retrieval representation or a different coded event, not another confidence/router variant over these continuations. | Both direct and compact calibrated target-substrate forms are negative. |
| Retired normalized WRT phrase-copy endpoint | Exact and normalized completed-event suffix tables predict next WRT codes; causal online context regret and frozen prefix-trained base/endpoint/bit calibration test abstention | `tools/wrt_normalized_phrase_copy_shadow.py`, `tools/wrt_normalized_phrase_endpoint_trace.py`, `results/fx2_reference_residual_v1/*phrase*json` | Unconditional `k12` loses `72` exact FX2 bytes. Online context regret saves `4` exact bytes (`8 B/1M`) but has negative holdout qbits and misses the `57.404 B/1M` debt before code. Frozen `8x8x8` and `16x16x8` calibrators lose `2` and `3` exact bytes with negative holdout. | None unchanged. A successor must add a new causal representation or features with disjoint transfer; do not tune phrase length, support, blend, or these router tables. | Exact rate is economically insufficient, held-out transfer is negative, and frozen calibration cannot retain the positive-event oracle. |
| Retired fixed full-CMIX complement | Frozen `625000`-ppm blend of exact `96x2` and continuously evolved full-CMIX post-SSE probabilities | `results/fx2_cmix21_matched_disjoint_terminal_v1/receipt.json`, `results/fx2_cmix21_lstm96_reproducible_source_package_v1/receipt.json`, `/home/x/enwiki9-nonproof/results/cmix21_full_teacher_geometry_1m_v1/matched_screen_receipt.json` | Opening-prefix exact replay saved `290 B/1M`; the same frozen blend saved only `61 B/1M` on the offset-`500M` same-store slice. Base archive and probability identity, clean guards, zero holdout regressions, and decoder replay pass. | None unchanged. Build a new WRT-native endpoint before any further selector work. | Terminal economics: the disjoint result misses the revised debt by `191.737 B/1M` before integration cost. |
| Retired unchanged original-order compact-`200` plus FX2-lite endpoint428 | Preserve compact-`200` as the archive-producing base and add only the dependency-closed FXCM slot `428` trajectory: byte-LSTM inputs, PPMD20M, quarter-sized second compound context, and deterministic Q16 integer-logit `31:9` blending | `results/cmix21_lstm200_fx2lite428_native_1m_v1/receipt.json`, `results/cmix21_lstm200_fx2lite428_context_recovery_10m_v1/receipt.json`, `tools/cmix_aux_logit_blend_screen.py`, quarantined source/package/runtime artifacts under `/home/x/enwiki9-nonproof/results/` | Exact `1M` archive identity, roundtrip, determinism, clean builds, source reconstruction, and decimal tree RSS pass. Bounded context recovery carries the candidate through exact `10M`, where archive `1,635,695` saves `1,818` bytes (`181.8 B/1M`) over compact-`200` but misses the counted ceiling by `575`. The `349,195`-byte package projects `109,557,404`, leaving `57,404` bytes to target. | None unchanged. Build an observation-neutral matched base/endpoint trace, freeze blend or causal reset/regret policy on training/development rows, and require at least `57.404 B/1M` net incremental gain plus any added source bytes on sealed rows before another native gate. | Terminal exact-`10M` economics miss: observed endpoint rate `181.8 B/1M` is below the counted `239.204 B/1M` floor. No unchanged `1G` gate. |
| Prepared neutral-FXCM compact ablation | Preserve the compact downstream trajectory and `431` mixer slots while removing only the compound FXCM feedback model and its object/static state | `/home/x/enwiki9-nonproof/cmix21-lstm200-neutral-fxcm-p1-trace-v1/`, `/home/x/enwiki9-nonproof/results/cmix21_lstm200_neutral_fxcm_source_package_v1/receipt.json` | Deterministic `222,361`-byte source ZIP; two clean builds match the `1,297,496`-byte reference backend. Known non-FXCM primary state is about `2.97 GB`. This is package and dependency evidence, not a compression result. | After the full-endpoint `10M` gate, run one guarded opening trace and require exact FX2 replay, stable holdout savings above the `1,226.871 B/1M` optimistic floor, and standalone RSS compatible with FX2 coexistence. | Opening signal misses the accounted floor, the compact trajectory needs FXCM feedback, or summed/native coexistence exceeds decimal `10GB`. |
| Proposed shared-FXCM compact mixer | Reuse the exact live `431` FX2 FXCM probabilities as the compact-`200` model's ordered FXCM inputs while continuously evolving its secondary contexts, bounded PPMD, two-layer byte LSTM, mixers, and SSE | `/home/x/enwiki9-nonproof/results/fx2_full_attribution_trace_1m_v1.bin`, `/home/x/enwiki9-nonproof/cmix21-lstm200-neutral-fxcm-p1-trace-v1/`, `/home/x/enwiki9-nonproof/results/cmix21_fx2_component_dependency_ledger_v1.json` | Source inspection proves both vectors have `431` ordered slots and that the FX2 producer already evolves its compound FXCM dependencies continuously. No shared-input codelength or RSS result exists yet. | Replay the exact existing attribution rows as a discovery surrogate, compare against both full and neutral endpoints on the same WRT bits, then replace the trace with an in-process reference to the live FX2 vector. Require held-out gain above the counted original-order/package floor, deterministic replay, and decimal-`10GB` coexistence. | Shared-input holdout gain misses the floor, component ordering/semantics are not identical, or the remaining compact state does not coexist with FX2 under the official memory boundary. |
| `cmix21` memory-shaped text mode | PAQ/cmix-style context mixing, PPMD, match modeling, SSE/APM calibration | `CMIX21_LOCK_SAFE_QUEUE.md`, `docs/cmix21_memory_valves.md`, `docs/cmix21_fx2_family_trace_correction.md`, `tools/cmix21_package_candidate.py`, `tools/cmix21_gate_decider.py` | The unchanged package is retired by its `14,864,716`-byte `100M` archive. The original-order full teacher was complementary, but the exact geometry `96x2` blend retains only `290 B/1M` overall before costs. | Use CMIX21 only as a teacher for genuinely new WRT-native endpoints; do not launch unchanged CMIX21 or tune the measured endpoint universe. | A new endpoint still misses the headroom floor, dependency closure requires the full slow ensemble, or a successor fails exact archive/memory/roundtrip/determinism gates. |
| Retired PAQ-free FX2/CMIX21 recurrent endpoint | FX2 article geometry-title/WRT plus FXCM, bounded PPMD, and continuously updated byte-LSTM endpoints with redundant PAQ removed | `docs/fx2_cmix21_nopaq_lstm_frontier.md`, `results/fx2_cmix21_lstm112_plus80_terminal_v1/receipt.json`, `results/fx2_cmix21_source_package_accounting_v1/receipt.json`, quarantined recurrent receipts under `/home/x/enwiki9-nonproof/results/` | Exact `10M` source-wrapper identity, roundtrip, determinism, and RSS pass. The `264,646`-byte reproducible package saves `23,619` counted bytes, but on the matched offset-`500M` reset slice the codec saves only `353 B/1M` and only `4 B/1M` over native `112`. | None unchanged. Re-enter only through a genuinely new WRT-native endpoint with disjoint target-rate margin and runtime eligibility. | This endpoint and secondary-width tuning are terminally retired from promotion. Preserve the wrapper and package as constructive evidence. |
| Retired static WRT dictionary boundary swap | Reassign low-frequency two-byte WRT slots to high-frequency three-byte words while preserving reversible dictionary lookup | `results/wrt_static_boundary_swap_112plus80_terminal_v1/receipt.json`, `tools/seal_wrt_static_boundary_swap_112plus80_gate.py` | Proxy saved `546` gzip bytes, but exact `112+80` transfer regressed `1,140` archive bytes at `10M` (`-114 B/1M`) under a clean guard. | None for this mapping. A different representation must first beat the exact codec, not only a storage proxy. | Exact archive transfer is negative; no replay or larger gate. |
| FX2-SC causal residual/SSE | Context mixing plus secondary symbol estimation; Wikipedia parser state as outer calibration only | `FX2_SC.md`, `FX2_SC_PAPER.md`, `docs/shadow_coder_spec.md`, `docs/residual_shadow_matrix.md`, `docs/fx2_residual_xml_ledger.md`, `docs/wrt_wiki_shell_v1.md`, `tools/fx2_xml_residual_screen.py`, `tools/fx2_residual_xml_ledger.py` | The legacy calibrated ledger has zero qualifiers. `wrt_wiki_shell_v1` now reconstructs page/title/prose/ref/URL/list/template/number/section state with archive-identical observation. Its combined shell SSE saves zero exact confirmation bytes, and its shallow hash-bucket retrieval loses `3` held-out bytes with every attributed band negative. | Replace hash buckets with decoder-rebuilt page-scoped WRT token tries, explicit copy distance/match length, and regime-specific causal routing; require held-out gain above `10,351.886200` calibrated 10M bytes for a 12KB component. | Confirmation remains non-positive, code/table cost consumes the gain, block regressions remain broad, or the decoder cannot rebuild the memory from its prefix. |
| Causal schema trie / seed dictionary | Adaptive dictionaries and PPM-style online history, but derived only from decoded history | `docs/algorithm_cards.md`, `docs/shadow_coder_spec.md`, `FX2_SC.md` | Design-only in this checkout. | Shadow coder proves exact-byte savings from trie priors with bounded memory and counted code. | Trie grows beyond the memory budget, predicts weakly outside source blocks, or needs future corpus metadata. |
| Embedding-teacher ordering/routing | Offline semantic clustering and article-family discovery; final payload must be deterministic rules | `docs/embedding_teacher_rules.md`, `docs/streaming_retrieval_block_teacher_manifest.jsonl`, `tools/embedding_teacher_order.py`, `tools/hierarchical_chunk_embedding_teacher.py`, `tools/hierarchical_retrieval_shadow.py` | Direct hierarchical-retrieval probes remain weak. GEPA `(template,mh2,mh4)` was rejected by the real canonical `10M` target ceiling. The useful teacher role is residual-regime discovery: dictionaries, routing, retrieval layout, page-structure rules, and prefix-rebuildable cluster assignments. | Teacher discovers a prefix-observable rule that beats the current substrate in exact shadow or replay after counted bytes and can be compiled into tiny deterministic decoder logic. | Improvement requires block IDs, full-block embeddings, labels, model/index bytes, teacher weights during decode, or uncounted external knowledge; the distilled prefix rule does not beat the posterior/control baseline. |
| I-SSA bounded attractor state | Robust integer state-space tracking as parser-state replacement | `I_SSA_LOCK_SAFE_REPORT.md`, `FX2_SC.md`, `tools/fx2_issa_shadow_search.py`, `docs/residual_shadow_matrix.md` | Bounded raw-log probe on `residual_apm_1m_mode_charclass_b050` found `1` held-out byte over `260,000` held-out rows. This is a weak positive shadow signal, not a feature-promotion result. | A bounded integer state improves held-out outer-SSE shadow bytes by enough to clear counted implementation bytes and then survives prefix replay. | State adds CPU/code cost without distributed held-out savings, or behaves like a noisy parser hash. |
| Deterministic MWCC router | Expert routing from causal rolling log-loss, with no transmitted route token | `RESIDUAL_ROUTER_LOCK_SAFE_REPORT.md`, `tools/fx2_mwcc_router_shadow.py`, `docs/shadow_coder_spec.md`, `docs/residual_shadow_matrix.md` | Bounded raw-log probe on `residual_apm_1m_mode_charclass_b050` saved `2` same-coder bytes but `0` held-out bytes across `260,000` held-out rows; current receipt is `flat_shadow`. | Router selection improves held-out shadow-coded bytes after counted table/code size and does not destabilize replay. | Expert overhead exceeds measured savings, held-out savings remain flat, or selected experts collapse to base model. |
| Descriptor/embedding model payloads | Functional tensor descriptors and neural compression research | `docs/embedding_teacher_rules.md`, external model-runtime docs outside this Hutter lane | Out-of-scope for final payload unless distilled into tiny deterministic logic. | Only distilled deterministic compressor logic enters the Hutter candidate with counted bytes. | A neural model, embedding index, or descriptor shard must be shipped and its byte cost exceeds archive savings. |

## Reading Order

1. `docs/status_receipt.md` for the active gate.
2. `docs/same_r_hutter_strategy_audit.md` for the matched search contract and mechanism priorities.
3. `docs/streaming_retrieval_mixer.md` for the primary SRSTC strategy.
4. `docs/algorithm_cards.md` for plain mechanisms and current scores.
5. `docs/best_results.md` for compact exact top rows by measured scope.
6. `CMIX21_LOCK_SAFE_QUEUE.md` for the active memory-valve decision tree.
7. `FX2_SC.md` and `FX2_SC_PAPER.md` for residual/SSE and sidecar components.
8. `docs/shadow_coder_spec.md` before trusting any residual or router claim.

## External Anchors

These are the primary or near-primary references used to keep the local
strategy register honest.

| Source | Link | Local use |
|---|---|---|
| Large Text Compression Benchmark rules | https://www.mattmahoney.net/dc/textrules.html | Confirms that the score counts compressed `enwik9` plus the decompressor archive and runtime files. |
| Large Text Compression Benchmark table | https://www.mattmahoney.net/dc/text.html | Keeps result language aligned with benchmark scope and accounting. |
| cmix reference page | https://www.byronknoll.com/cmix.html | Anchors the context-mixing, single-pass bit prediction, SSE/APM, and arithmetic-coding substrate. |
| cmix source repository | https://github.com/byronknoll/cmix | Source reference for the `cmix21` family being memory-shaped here. |
| fx2-cmix repository | https://github.com/kaitz/fx2-cmix | Public Hutter-family reference for NLP, online reverse dictionary transform, Wikipedia transform, and article ordering. |
| Hutter Prize FAQ | https://prize.hutter1.net/hfaq.htm | Scope and competition context; local claims still require full `1G` official-accounting receipts. |

## Promotion Discipline

Do not promote an idea from this register because it is elegant. Promote only
when the receipt type matches the claim:

| Claim | Required receipt |
|---|---|
| Exact prefix candidate | Driver result JSON with roundtrip true. |
| Deterministic prefix candidate | Driver result JSON with determinism true or byte-equal replay. |
| Memory-safe candidate | RSS guard receipt under the selected guard. |
| SRSTC feature | Streaming retrieval shadow receipt with counted implementation bytes, deterministic table update hashes, held-out net savings, and conditional attribution for copy/typed/byte-prior controls when copy is enabled. |
| Residual/SSE feature | Shadow-coder receipt with counted implementation bytes. |
| Full target claim | Full `1G` official-accounting receipt with score at or below `109,500,000`. |

Evidence ladder for teacher and residual work:

| Level | Evidence | What it can decide |
|---:|---|---|
| 1 | Proxy or teacher forecast | Which experiment to run. |
| 2 | Shadow replay against the exact fx2 residual stream | Whether a signal transfers to the target substrate. |
| 3 | Constructive prefix receipt with decoder code counted | Same-scope candidate viability. |
| 4 | Multiple disjoint block receipts | Regression and transfer risk. |
| 5 | Full `enwik9` archive, roundtrip, determinism, memory, and official accounting | Score claim. |

Savant-style distillation boundary: offline teacher outputs, embeddings,
posterior traces, and oracle routing are discovery tools. A final Hutter
candidate may use only the distilled deterministic mechanism that the decoder
rebuilds from the already-decoded prefix, with every byte of code, table, and
configuration counted.
