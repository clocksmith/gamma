REJECT

Reasoning:
The provided examples (1-12) rely heavily on specific historical facts,
dates, names, and contextual details that are not universal across all pages in
the same domain. These examples do not exhibit a recurring, syntactically
deterministic pattern that can be generated byte-exactly from a fixed set of
rules without referencing external knowledge or specific page content. The
failure of these examples to support a deterministic cross-page rule means no
such language can be proposed. The request requires a new deterministic
language for reusable fixed-surface generation, but the current data set
contains no such surface. Therefore, reject rather than inventing a rule that
does not exist in the provided examples.

Receipt note: this is a normalized preservation of the relevant model answer,
excluding loader and performance logs. It was generated at temperature zero
and seed 1 by the model and runtime bound in
`mobius2_residual_directed_concept_discovery.md`. It is discovery evidence only
and has zero compression score credit.

