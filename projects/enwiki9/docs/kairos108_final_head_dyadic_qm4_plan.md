# KAIROS-108 final-head dyadic opening gate QM4

Candidate: `kairos108_final_head_dyadic_qm4_v1`

Parent: `kairos108_final_head_dyadic_qm3_v1`

Status: frozen LLD visibility repair; zero score credit.

## Single change

QM3 compiled LLVM bitcode successfully but the BFD override could not link it
without an LLVM LTO plugin. The original cmix-obias donor contains the matching
LLD 17 executable and its `libxml2` compatibility library. This child exposes
those existing pinned paths during the unchanged `prof_use` build, restoring
the donor's original PGO/LTO linker semantics.

No observer record, modeled object, probability feature, rank, quantization,
atomic interval, correction law, dynamic program, control, schedule cost,
arithmetic coder, donor asset, or scientific gate changes.

## Required arms and decision

Run unchanged `B0`, `G0`, `K0`, `P0`, `R0`, `S0`, and `O0` over the first
`1,000,000` raw bytes. Require traced-parent payload identity, exact `B0` and
`K0` replay, repeat identity, legal probabilities, schedule no larger than
`128 KiB`, at least `4,500` K0 gross bytes, positive chronological thirds, and
at least `500` bytes over every matched control.
