# HELICAL WRT Far-History Discovery QM3

## Question

QM2 proved that raw-distance discovery mostly selects sources already inside
Endpoint428's 100M modeled-byte ring. Does the exact full WRT stream itself
contain a target-bearing reservoir of event-aligned exact repetition beyond
that ring?

## Frozen scanner

QM3 scans the receipt-bound 647,798,592-byte WRT stream using the established
32-byte rolling window, low-six-bit anchor, two independent 64-bit hashes,
exact comparison, earliest source, 100,000,000-byte minimum distance, 64-byte
minimum match, maximal extension, fully closed source, and chronological
nonoverlap. Starts and ends are clipped inward until both source and target are
canonical WRT event boundaries. No raw match candidates are imported.

Every selected record is encoded as canonical ULEB target gap, WRT distance,
and WRT length. The three columns and header receive one deterministic LZMA
ledger. Two complete scans, raw ledgers, and compressed ledgers must agree.

## Gate

The free archive-average gross must exceed the 4,389,323-byte debt plus a
500,000-byte reserve and remain positive in every WRT-stream third. A free pass
with a paid-ledger miss authorizes source-aligned corridor optimization over
this exact parent-coordinate universe. A paid bare-margin pass authorizes an
exact residual integration before any full P1 trace. A free miss retires this
WRT far-history universe without anchor or threshold sweeps.

This is zero-credit archive-average evidence until actual selected-span P1 cost,
an exact residual/inverse, counted source, runtime, memory, and deterministic
full-1G archive receipts exist.
