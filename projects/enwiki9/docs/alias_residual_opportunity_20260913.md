# Natural abbreviation opportunities after the strong parent

Candidate `alias_residual_opportunity1m_q0_v1`, owner `root_alias_residual`,
continues the [synthetic alias result](alias_context_research_20260912.md).
Its constructed transfer does not establish natural-text coverage or gain over
the strongest predictor. The suffix alphabet's completed archive regression
also rejects treating an intuitive word relationship as sufficient evidence.

Deliberately selected lenses 2 and 9 ask whether explicit equivalence supplies
information that the retained strong parent has not already priced cheaply.
The three considered directions are:

- Explicit abbreviation edges: select an opportunity census on existing exact
  parent probabilities before implementing a natural-text expert.
- Another generic typed continuation mixture: reject unchanged. The prior
  [typed-event decision](../results/typed_event_sleeping_bayes_envelope_q0_v1/decision.json)
  beats its blind control but loses to the parent; correct associations alone
  did not make that predictor competitive.
- Changed research machinery or model-assisted search: retain as a possible
  future matched-search experiment. No measured discovery bottleneck justifies
  a new adapter, scheduler or framework for this question.

The retained [trace certificate](../results/typed_event_sleeping_bayes_parent_trace_q0_v1/decision.json)
binds two byte-identical P1 streams and archives, exact arithmetic payload replay
and the original WRT inverse. Its wrapper hash is the same one used by the best
counted forecast package. The native backend remains d1066630; this census
does not run it again. All input identities use complete hashes in the
[frozen contract](../operations/adaptive/experiments/alias_residual_opportunity1m_q0_v1.json).

Population: exposed raw `[0,1000000)`, with 600,742 WRT bytes including the
segment header. This is separate from cold `[819000000,820000000)` confirmation.
The retained parent was initialized cold using its original dictionary. No
candidate fitting, population selection or future-state initialization is added.

The observer accepts a two-to-six-letter uppercase abbreviation in parentheses
after a two-to-six-word name, at most 96 bytes long. Initials must match after
removing the fixed stopwords `a an and for in of on the to`. Definitions become
available only after the closing parenthesis. At most 16 aliases are retained
per article; a conflicting redefinition disables the alias until `<page>` reset.
No markup normalization, entity repair or linguistic model is used.

A mention is recognized only in its exact surface spelling followed by one
decoded ASCII space. Its target begins at the first completed WRT event boundary
for that raw offset. A donor is the most recent opposite-form eight-byte WRT
window, installed after the definition and entirely decoded before the target.
An unfinished latest donor does not expose older or future bytes. This deliberately
narrow grammar may have poor coverage; a zero result is scoped to that grammar,
window, history rule and population.

D uses the explicit edges. S rotates abbreviation edges over the distinct full
names known at the current boundary while preserving literal full-name histories.
S is unavailable with fewer than two distinct names. P remains the original
probability stream. K is an independent complete census replay. No prediction,
archive, parent state or dictionary code changes in this experiment.

The decoder-implementation pass checks when names, boundaries and donor bytes
become available. The falsifying-review pass tests transfer versus a disrupted
edge, future definitions, page resets, conflicting definitions and an incomplete
donor window. These are the author's checks, not an independent reviewer claim.
The five [causality tests](../tests/test_alias_residual_opportunity_v1.py) pass.

For each arm, retain every opportunity, donor agreement and the parent's ideal
bits on the union of target windows, avoiding double counting. That log loss
is an optimistic ideal-payload ceiling for changes confined to those windows.
It is not achieved gain or a proved finite-archive bound. The observer source
size is recorded as analysis machinery, not a measured final decoder package.
Missing control opportunities remain unavailable rather than zero-gain verdicts.

Two independent census subprocesses must produce byte-identical numeric receipts
and reconstruct the bound raw data through the existing WRT inverse. Bounds are
CPU 2, 1,073,741,824 memory bytes, 67,108,864 scratch bytes, zero swap and a
180-second elapsed stop. Each subprocess has a 60-second stop. Independent runtime
storage protects this execution from the shared root's prior ENOSPC incidents.

Zero eligible D windows rejects only this realization on this exposed sample.
A positive census permits evaluating a specifically priced causal expert;
it supplies no native-integration or larger-run result. There is no RSI claim:
this does not compare two research systems under matched search budgets.
Full-corpus score remains unknown, forecast 109,389,323 bytes, target 90,000,000
bytes and debt +19,389,323. Every observer result receives zero objective credit.
