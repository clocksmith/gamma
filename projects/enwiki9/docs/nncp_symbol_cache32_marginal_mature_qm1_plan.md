# NNCP mature addressless cache-32 symbol marginal

Candidate: `nncp_symbol_cache32_marginal_mature_qm1_v1`.

This is the single unchanged maturity transfer authorized by the exact
`262,144`-symbol cache result. It retains the frozen 32-symbol window,
symbol-level `16:1` base/cache marginal, balanced tree, and stream-17 control.
Cache histories reset only at the native complete-block boundary, matching the
receipt-bound trace geometry.

The population is all `1,998,848` symbols and `27,984,335` branch frequencies
from `teacher_native_trace.bin`. The trace is parsed in its recorded native
block/state/stream order and checked against the full preprocessed symbol
artifact. The candidate is encoded twice and decoded exactly. Independent
terminated original-coordinate thirds preserve the full causal model state.

The gate is:

```text
actual candidate gain versus faithful       >= 30,000 bytes
candidate gain in every chronological third  > 0 bytes
candidate margin over cross-stream control  >= 8,000 bytes
candidate repeat payload                     byte-identical
candidate arithmetic decode                  exact
compressed source                           <= 65,536 bytes
```

This remains a zero-credit teacher replay because the probability trace is not
decoder-generated. A pass authorizes one native source integration and an open
backend contract; a miss retires the cache mechanism without geometry or prior
sweeps.
