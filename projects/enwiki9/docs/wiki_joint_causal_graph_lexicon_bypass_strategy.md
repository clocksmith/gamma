# WIKI-JOINT Causal Graph-Lexicon Bypass Strategy

Status: parked composition contract, zero score credit, no adaptive proposal or
job. Materialize only after contributing isolated lanes pass their own gates.

## Role

WIKI-JOINT is not a fourth information source. It is the exact composition
contract for paying decoder-built event sources such as:

- WIKIBACK prior incoming-anchor contexts;
- WIKISECTION prior same-heading section bodies;
- WIKIFORWARD earlier decoded destination-page lexicons;
- a later WIKIGRAPH target-identity source only if its independent paid gate
  passes.

At an eligible exact WRT-event boundary, each active source produces a bounded
prefix-causal snapshot. The codec forms one canonical union over exact encoded
WRT event byte strings, merges duplicate identities, aggregates only
pre-truth weights, and emits one finite `HIT + rank` or `ESCAPE`. A hit
reconstructs the exact encoded event; an escape consumes the event from the
parent arithmetic payload.

This joint replay is mandatory because isolated savings overlap and because
side coding changes arithmetic termination. Never add isolated WIKI gains.

## Causal and decoding contract

Every source record is published only at its frozen completed boundary. The
union, activation, capacity, duplicate handling, weights, ties, eviction, and
memory cap are deterministic functions of the decoded prefix and are frozen
before current truth.

Candidate identity is the exact encoded WRT event, not a decoded lexeme or
semantic label. The event grammar and framed finite side/residual streams must
give one unique next event at every position and reconstruct both complete WRT
and raw corpus bytes.

## Native state-preservation contract

For every bypassed bit, execute the complete ordinary parent path:

```text
p = Parent.Predict(state)
discard p for arithmetic coding
Parent.PerceiveAndUpdate(p, reconstructed_truth_bit)
```

This includes cached forward values, recurrent work, mixers, SSE, FXCM,
matches, PPM state, asynchronous endpoint synchronization, WRT state, and Wiki
parser/graph state. A partial bit-injection update is insufficient.

After every hit event, quiesce deferred work and compare:

- canonical logical parent predictor-state hash;
- WRT decoder-state hash;
- Wiki parser and graph-state hash.

Exclude only arithmetic-coder interval/output state, which must differ when
truth bits are bypassed. A precomputed P1 trace proves trace accounting, not
native state equality.

## Exact accounting

Compare the eventual native child against the current constructive parent,
endpoint428, unless another parent is reintroduced with its complete package
charged:

```text
G_net = (|A_parent| - |A_child|) - delta_program
```

`A_child` includes the jointly terminated tag/rank side stream, residual
arithmetic stream, framing, and termination. `delta_program` includes every
new source, table, graph structure, and runtime dependency.

For Q24 rank frequencies, require a bounded candidate count `K < 2^24`, every
frequency nonzero, exact normalization, and canonical ties. Ideal KT or Q24
loss is diagnostic; actual finite payload bytes decide promotion.

Opening-10M free ceilings and 1G retention ratios are planning screens only.
They cannot enter `109,389,323` or receive source-bound score credit.

## Ordering

1. Resolve the active NNCP receipt.
2. Run the corrected WIKIBACK finite gate.
3. Resolve WIKISECTION QM1 and any paid child it authorizes.
4. Resolve WIKIFORWARD QM1 and any paid child it authorizes.
5. Measure overlap among only the paying frozen sources.
6. Freeze one union format and run a fresh exact 10M joint replay.
7. Prove native state equality before distant or 100M promotion.

WIKI-JOINT does not solve runtime by itself: full prediction/update still runs
for every bypassed bit, with additional graph and side-coder work. Runtime and
memory remain independent terminal gates.

## Lightweight-parent complementarity gate

A paying WIKI source improves representation but does not remove endpoint428's
predict/update work. It therefore cannot by itself resolve the one-core runtime
gap. After the first isolated WIKI child clears its actual finite 10M package
gate and frozen transfer gate, preregister one dependency-closed lightweight
parent `L` selected for materially lower modeled work. Do not select among
multiple lightweight parents after observing WIKI results.

Run four exact references on identical populations:

```text
F       full endpoint428 parent
F+W     full parent plus the frozen paying WIKI source
L       lightweight parent without WIKI; required attribution control
L+W     lightweight parent plus the identical WIKI source definition
```

The primary architecture comparison is `F` versus `F+W` versus `L+W`; `L` is
mandatory because it measures how much information `W` recovers on the changed
probability trajectory. Every arm must build its own actual terminated parent
or residual payload, side stream, frame, package, P1 trajectory, decode,
inverse, repeated archive, and resource receipt. The isolated `F+W` gain may
not be subtracted from `L`'s loss, and its hit schedule or rank distribution may
not be reused without replay if `L` changes an event's literal economics.

Freeze before execution:

- the exact dependency-closed components removed or reduced in `L`;
- all state and feature inputs remaining available to `L` and `L+W`;
- the WIKI event language, causal publication rules, and memory cap;
- source and model package accounting for all arms;
- one-core process topology and encode/decode measurement contract;
- score, transfer, runtime, memory, and kill inequalities.

A pass must show that `L+W` is target-bearing through its own fresh archive and
ledger while materially reducing measured one-core work. A result in which
`F+W` saves bytes but `L+W` misses the score or resource gate remains a useful
representation result, not a prize-eligible architecture.

## 100M portfolio gate

Do not use 100M for discovery. Promote only exact 10M plus distant winners into
three independent roles when evidence exists:

1. representation: the strongest isolated WIKI child or paying WIKI-JOINT on
   `F`;
2. prediction: a compact NNCP-derived student only after two teacher blocks and
   a paid student screen;
3. replacement composition: the same paying WIKI source on the frozen `L`.

For 100M archive gain `g_100`, incremental program cost `delta_P`, and a safety
reserve `R` fixed before the run, require:

```text
10 * g_100 - delta_P >= 1,389,323 + R
```

Every 10M and 100M receipt must report signed projected margins to the standing
`108,000,000` target and the unverified `107,407,896` competitive contingency.
Only the standing target governs while the external record remains unverified.
