# MIXREGRET-CERT: Component-Union Closure Certificate

Status: CLAIMED / ZERO SCORE CREDIT

## Question

Does the selected endpoint428 implementation already compute enough distinct
decoder-visible probability information to recover at least 2,000 bytes per
million raw bytes through a causal selector, or must the project replace the
predictive architecture?

MIXREGRET is a closure certificate, not an open-ended mixer sweep. It first
measures truth-aware union headroom, then decoder-visible selectability, then
fully paid block routing. A failed gate retires unchanged component
recombination.

## Exact trace

The observation-only build derives from the selected source-bound package
`cmix21-lstm200-plus-fx2lite428-onlinepairlayer0-v17`. Before truth is learned,
each row freezes:

- the exact final parent P1 (`M0`);
- the fixed-blend input to the online residual mixer;
- all 26 layer-0 endpoint probabilities;
- the base post-SSE endpoint;
- the FX2 endpoint428 probability.

Truth is appended only when `Perceive(bit)` begins. The trace path is selected
with `CMIX_MIXREGRET_TRACE`. Trace-on and trace-off archives must be byte
identical, and exact range replay of `M0` must reproduce the parent arithmetic
payload byte for byte.

## Gate U0: component union

At each row, choose the smallest binary log-loss among `M0` and all component
probabilities. Replay the chosen probabilities through the exact CMIX 32-bit
range coder.

Run three null controls. Keep `M0` aligned and circularly shift every other
endpoint by endpoint-specific multiples of 8,192, 139,264, and 827,392 rows.
The multiples preserve bit position and each endpoint's marginal and temporal
trajectory while breaking its alignment with truth.

Authorize U1 only when:

- exact U0 savings are at least 6,000 B/M;
- real U0 exceeds the strongest shift control by at least 1,000 B/M;
- parent payload identity is exact.

Otherwise retire component recombination without feature, blend, bucket, or
capacity sweeps.

## Gate U1: decoder selectability

If U0 passes, freeze a bounded context partition on chronological development
pages using only pre-truth state:

- bit position and partial-byte prefix;
- WRT/XML phase;
- final-confidence bucket;
- component-disagreement signature;
- match/support buckets;
- recent causal component regret.

The frozen partition must save at least 3,000 B/M on chronological and distant
holdout after its description cost. U0 receives no score credit.

## Paid selectors

If U1 passes, evaluate labels paid before payload blocks of 1,024, 4,096, and
16,384 raw bytes and one complete page. Charge label entropy, framing, coder
termination, and source allocation. At least one selector must clear
2,500 B/M before CTS-MIX is authorized.

## Constructive candidate

CTS-MIX uses a bounded context-tree switch over `M0`, distinct components, and
predeclared component-family mixtures. Native integration requires:

- at least 60 percent of U1;
- at least 2,000 B/M net;
- at least 23KB canonical-10M net;
- positive distant populations;
- no window regression greater than 128 B/M;
- exact roundtrip and deterministic replay;
- compressed source growth no more than 40KB preferred.

Output gating receives no runtime credit. Prediction skipping or full component
deletion requires native state replay and measured mature-cycle reduction.

## Claim boundary

Trace, U0, U1, and paid selectors are research evidence only. Oracle or shadow
savings never change the official score, forecast, target debt, or runtime
certificate.
