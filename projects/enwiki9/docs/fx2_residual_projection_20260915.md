# Paid final-coder affine correction

This prospective development question follows the closed head-transport rejection,
not another mixture of its fixed probability streams. The trimmed release P is
the comparator. No downloadable external prototype exists in this environment;
the supplied equations are independently implemented and tested here.

For exact parent bit-one count c, Q=65536, bit position r and pre-truth feature
vector phi, q=p+p(1-p)theta[r].phi. Eight32-coordinate rows have L1 radius1/2.
The declared table is256 signed16-bit integers,512 bytes before headers, program,
runtime or any required copies. Fixed scale S=32768 and sum(abs(k_j))<=16384
enforce the domain. Complete positive count rounding uses nearest/ties upward,
clamped1..65535, with signed64-bit intermediates. Zero coefficients preserve all
65535 valid counts. Coefficients, if fitted, are transmitted information.

Each feature is the sign of the ordered binary64 sum of six consecutive binary32
coordinates of the parent's final normalized192-vector xn. Groups are fixed
before observing the corpus values. Positive,zero,negative become1,0,-1. Capture
occurs at the native output head after processing an already decoded token. The
same features predict the next eight coded bits. Initial and PPM-only next-token
features are zero; the native piece-final branch invalidates them before reuse.
No parent probability, cached learning input, reset, optimizer or model is changed.

The likelihood identity q(y)/p(y)=1+theta.phi*(y-p) gives ideal saving in nats
F=sum ln(1+theta.x), x=phi*(y-p). Its Hessian is the negative sum of scaled outer
products, so F is concave. The supporting plane at zero gives
F*<=sum_r max_j(abs(sum_t phi_tj*(Q*y_t-c_t)))/(2Q).
The integer sums are an exact certificate over this declared feature family.
For a chosen feasible vector, sum(z-z*z)<=F for z=theta.x in[-1/2,1/2].
Neither inequality proves global compressor optimality or90M.

Probability rounding is bounded separately. In truth-count units the smooth
count is at least c_truth*(Q+c_truth)/(2Q). Rounding and endpoint clamping raise
it by at most1/2. Hence additional ideal log gain is at most
Q/[c_truth*(Q+c_truth)] by ln(1+a)<=a. Sum over nonzero-feature events only,
round each histogram contribution upward on a shared2^-48 grid, and add this
exact rational upper bound. Conversion to bits uses ln2=2*atanh(1/3),32 positive
terms and a rational geometric remainder. Floating output is diagnostic.
Finite arithmetic termination and implementation/package costs remain separate.

Candidate `fx2_residual_features250k_v1` performs one native build and four corpus
phases: observed encode, independent decode, encode from restored raw, and
unobserved encode. Every archive must match the existing33429-byte parent.
Every28-byte coder record must match the retained parent; each12-byte feature
record contains32 packed ternary signs,actualQ16 count,bit position,validity and
truth. The feature snapshot is taken before decoding truth and written before
parent Perceive. Decode and repeat feature files must match exactly. Hashes of
hidden values alone cannot substitute for these observations. Full original
parameter/optimizer state is not serialized; no stronger state claim is made.

The exposed raw interval is[0,250000), with151210 modeled WRT bytes. No reserved
population is read. One fixed grouping and one coefficient domain are declared.
Both certificates must repeat exactly. If even the rounded upper cannot pay
4096 coefficient bits, reject this family's ideal-plus-fixed-table economics
without fitting. That is not a finite-archive impossibility theorem or a claim
against compressed/sparse tables, other features or another population. A higher
bound authorizes bounded fitting only, not a positive compression claim. Fitting,
quantization, finite replay, native integration and confirmation each need their
own measured evidence. No16-phase treatment integration precedes this measurement.

The next integration, only if paid finite replay supports it, preserves original
parent learning: P, K zero correction, D paid fitted coefficients, S equally
priced coefficients fitted under prospectively misaligned truths. Permuting
features and refitting is not a valid negative control. No prior34-byte archive
gain or executable reduction is inherited without a matched delivered child.

Target90000000 complete bytes; verified full1G unknown. Historical109389323
forecast remains planning evidence only. Objective credit0; complete package,
runtime qualification and official submission result remain unproved.
