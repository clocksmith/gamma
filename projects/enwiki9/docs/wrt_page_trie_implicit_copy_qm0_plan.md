# WRT page-trie implicit copy QM0

Status: terminal zero-credit rejection. T0 is `-275,600.140` gross and
`-283,348.140` after its `7,748`-byte source package at canonical `10M`.

## Objective

Test whether decoder-rebuilt page/regime-scoped WRT token sequences support a
paying copy alphabet against the exact Endpoint428 parent. This is not another
next-byte histogram, point-mass blend, static FORM dictionary, or explicit
occurrence ledger. The decoder derives every source from completed history;
the coded stream pays every literal/copy decision and copy length.

## Causal schedule

After each complete WRT event, both encoder and decoder update an exact event
sequence and Wiki regime state. At the next event boundary they find the
longest suffix, capped at eight complete events, that occurred earlier in the
same regime. A source is eligible only when its next event is unique across all
bounded matches; the most recent matching source breaks residual ties. The
source and opportunity position are therefore implicit.

The arithmetic alphabet is `literal` plus copy lengths `1, 2, 4, 8, 16, 32`.
Counts are adaptive and keyed only by decoder-visible regime, suffix length,
and support bucket. A copy outcome is legal only when that many upcoming WRT
events exactly match the derived prior source. Literal events retain the exact
parent P1 probabilities. Skipped copy events still update the parent model in
a native realization; trace replay uses the already-bound later parent P1 rows
because reconstructed truth is identical.

## Frozen arms and proof

- B0: exact Endpoint428 parent arithmetic replay.
- C0: identical source/macro machinery with regime identity removed.
- S0: deterministic shuffled-regime control.
- T0: typed page/regime-scoped source selection.

The tool must run twice deterministically, decode every arm, reproduce the WRT
store and raw `10M` input, and bind the Endpoint428 P1/archive receipts. Source
package bytes and all macro symbols are counted. No oracle-only correct-copy
filter is allowed.

## Economics

The current forecast is `109,389,323`, leaving `4,389,323` bytes to the
`105,000,000` target. Promotion requires T0 to save at least `50,000` net bytes
at canonical `10M` after its compressed source package, remain positive in
every chronological third, and beat C0 and S0 by at least `10,000` bytes. Any
inverse, determinism, provenance, or memory failure is terminal. A miss retires
this exact suffix cap, source rule, alphabet, and adaptive context without
parameter sweeps.

## Result

T0 displaced `60,279.148` Endpoint428 bytes, but its paid selectors cost
`141,962.162` bytes and paid lengths cost `193,917.126`. All chronological
thirds are negative. T0 beats C0 by `14,740.420` bytes but trails S0 by
`26,589.122`. The exact WRT/raw inverse and parent provenance pass; the retry
completed with peak RSS `2,088,104` KiB and no decimal-limit overage. Retire
this source rule, suffix set, selector model, and copy-length alphabet without
exact mixed-payload or native successors.
