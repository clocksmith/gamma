# Confirmation of the unchanged released expert mixture

Candidate `fx2_expert_confirm1m_v1` tests the released mixture on one existing
1,000,000-byte population. This is a separately bounded research comparison,
not an automatic promotion of its held package or a new algorithm.

The development archive gain was 34 bytes, and the disjoint cold 250KB gain
was 11 bytes. Both used the same released implementation. Its measured extra
source ZIP plus required compile option costs 1,188 bytes; the alternative
executable increment is 4,096 bytes. These costs remain charged and are not
multiplied by the number of development samples. No full-corpus gain follows
from either local measurement.

The preceding interrupted-match screen is independently retired. It contributes
no source, state, donor, probability, or gain to this candidate. This decision
uses the workflow's testing branch: measure an unchanged positive implementation
instead of introducing another unmeasured mechanism.

## Frozen population and implementation

Use the existing raw interval `[713000000,714000000)`, SHA-256
`20b4d8d7e140ccf799ed9af127d03a42af006efc97bd38e3cf2528cd611aaf13`.
It was already used by native FX2, opcode baselines and trim confirmation.
It was not used to fit or select this mixture; it is not globally untouched.
Initialize cold with the unchanged model and dictionary pretraining. No earlier
corpus state is supplied. This tests transfer at this scope; it does not isolate
an effect of longer history from differences in sample content.

The retained transformed input has 653,306 bytes, including its ten-byte
frontend framing. P must reproduce the existing 131,238-byte native archive.
Read the exact released P/D ZIPs, compile commands and executable hashes from
`fx2_expert_release250k_v3`. Rebuild both twice and require released binary
identity. No source, feature, prior, update, mixture strength or model changes
are allowed. Development K/S controls remain inherited evidence; they are not
rerun or claimed as fresh mechanism attribution on this population.

For each arm, build, clean, rebuild, preprocess, encode, independently decode,
then encode the restored raw file. The fourteen phases must prove the original
transformed bytes, exact raw inverse, repeated archives, and immutable released
executables. The decoder requires the counted model and dictionary, but no
probability trace or external prediction service.

## Resources and decisions

The prior trim confirmation measured approximately 124 seconds per native 1MB
codec operation on this same population. Retain its 600-second per-codec stop,
360-second build stops, CPU2 assignment, 9,999,998,976-byte memory ceiling,
zero swap, 16GB logical scratch ceiling and 3,600-second aggregate stop.
Those are execution bounds, not completion promises. Timing is diagnostic on
the shared host. Publish source and ownership, then obtain fresh admission.

Let `g = archive(P) - archive(D)`. Report `g`, `g - 1188` and `g - 4096`
separately. Source delivery and executable delivery are alternative component
prices, not additive charges or complete accepted submission scores.

- If `g > 1188`, the measured source-and-option component pays locally. This
  permits consideration of a separately specified 10MB comparison, with full
  package closure and resource admission still outstanding.
- If `0 < g <= 1188`, retain the positive archive result under the existing
  package hold. Do not automatically enlarge the scope.
- If `g <= 0`, retire this fixed confirmation configuration. Do not tune on
  these bytes or select another population to rescue it.
- An inverse, identity, resource or required-evidence failure is incomplete
  execution, not a valid compression loss.

No result here establishes a complete full-corpus score, a winning forecast,
novelty, license closure, or official eligibility. The target remains exactly
90,000,000 complete bytes; the measured parent distance to it is unknown.
