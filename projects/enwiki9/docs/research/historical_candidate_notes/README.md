# Historical Candidate Notes

These files preserve early experiment-family notes whose animal names match
program IDs in `programs/` and result IDs in `results/`.

They are historical research records, not current project status. Current
algorithm state belongs in `ALGORITHMS.md`, generated rankings belong in
`docs/evidence_matrix.md`, and active operator state belongs in
`docs/status_receipt.md`.
