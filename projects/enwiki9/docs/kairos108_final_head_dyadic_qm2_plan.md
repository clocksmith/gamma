# KAIROS-108 final-head dyadic opening gate QM2

Candidate: `kairos108_final_head_dyadic_qm2_v1`

Parent: `kairos108_final_head_dyadic_qm1_v1`

Status: frozen host-toolchain infrastructure repair; zero score credit.

## Single change

QM1 passed the queue-directory repair and applied the frozen observer patch,
then failed before compilation because its Make invocation named
`clang++-17`, which is absent from `PATH`. The pinned Clang 17 compiler and
libraries already exist under
`/home/x/enwiki9-nonproof/toolchains/clang17/root/usr`. This child supplies the
absolute compiler path and matching `LD_LIBRARY_PATH` during the unchanged
observer build.

No observer record, modeled object, probability feature, rank, quantization,
atomic interval, correction law, dynamic program, control, schedule cost,
arithmetic coder, donor asset, or opening scientific gate changes. The target
remains a complete counted score no larger than `108,000,000` bytes, and this
opening experiment receives no score or forecast credit.

## Required arms and decision

Run unchanged `B0`, `G0`, `K0`, `P0`, `R0`, `S0`, and `O0` over the first
`1,000,000` raw bytes. Require traced-parent payload identity, exact `B0` and
`K0` range replay, repeat identity, legal probabilities, and a paid schedule no
larger than `128 KiB`. The discovery references remain `4,500` gross bytes and
`500` bytes over every matched control, positive in every chronological third.

A complete scientific miss retires this KAIROS realization. Another
infrastructure failure may be retried only by a new identity with unchanged
science.
