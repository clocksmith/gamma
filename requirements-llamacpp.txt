# Requirements for the llama.cpp engine
llama-cpp-python>=0.2.57
# Note: Installation might require build tools (CMake, C++ Compiler)
# and specific CMAKE_ARGS for hardware acceleration (e.g., CUDA, Metal, OpenBLAS).
# Refer to: https://github.com/abetlen/llama-cpp-python
# `numpy` is in the base requirements.txt