# Requirements for the llama.cpp engine
llama-cpp-python>=0.2.50 # Check for latest version
# Note: Installation might require build tools (CMake, C++ Compiler)
# and specific CMAKE_ARGS for hardware acceleration (see README)
