# Requirements for the PyTorch engine
# Includes user-provided versions and dependencies
torch>=2.1.0
bitsandbytes>=0.41.1   # For 4/8bit quantization
accelerate>=0.27.2     # Often needed for device placement/quantization
optimum>=1.16.0        # May be used for optimizations/export
auto-gptq>=0.6.0       # If using GPTQ quantized models via transformers
# transformers is in the base requirements.txt
