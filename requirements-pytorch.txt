# Requirements for the PyTorch engine
torch>=2.1.0
# For quantization and advanced device mapping:
bitsandbytes>=0.41.3
accelerate>=0.27.0
# For Flash Attention 2 (optional, if selected via --pytorch-attn and supported by hardware/model)
# optimum>=1.16.0
# flash-attn # Installation can be complex, platform-dependent.
# `transformers` is in the base requirements.txt