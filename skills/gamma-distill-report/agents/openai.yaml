interface:
  display_name: "Gamma Distillation Reports"
  short_description: "Rebuild normalized distillation reports"
  default_prompt: "Use $gamma-distill-report to rebuild Gamma's translation-distillation reports from existing artifacts."
