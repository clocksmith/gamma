interface:
  display_name: "Gamma Embedding Distillation"
  short_description: "Run a declared embedding pipeline"
  default_prompt: "Use $gamma-embedding-distill to launch, resume, or evaluate this declared embedding-distillation pipeline."
