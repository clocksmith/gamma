# GAMMA/requirements.txt
# Core requirements for GAMMA, common across engines.
# Individual engines have their own requirements-*.txt files.

transformers>=4.38.0 # For tokenizers (used by most engines) and some model classes
numpy>=1.23.0       # For numerical operations, array conversions

# Optional, for color support on Windows if not using a modern terminal
# that supports ANSI escape codes directly.
colorama>=0.4.6