# Core requirements (if any beyond Python built-ins)
# Currently, core logic might rely on transformers for tokenizers even if engine is different
# Let's include transformers here as a common dependency.
transformers>=4.37.0
numpy>=1.21.0 # Often useful, pulled in by many libs anyway
colorama>=0.4.6 # Optional, for Windows color support
